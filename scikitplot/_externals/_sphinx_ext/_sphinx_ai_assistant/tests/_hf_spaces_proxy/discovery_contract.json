{
  "$schema_note": "Canonical privacy-minimized server -> client discovery contract. GET / emits only coarse collection/readiness signals required by the browser. Exact routing URLs, repository identifiers, storage links/topology, and credential classes are not public discovery data.",
  "version": 3,
  "discovery_endpoint": "GET /",
  "token_policy": "No secret values or credential classes are public. Token fields remain schema-compatible but are emitted as 'unknown'; only least_privilege_mode exposes a coarse separation boolean.",
  "consumed_fields": [
    {
      "server_path": "training.dataset_repo",
      "server_key_literal": "\"dataset_repo\"",
      "client_access_expr": "tr.dataset_repo",
      "client_field": "repoId",
      "purpose": "Schema-compatible field intentionally emitted as null; user-visible dataset identity requires explicit site/profile configuration."
    },
    {
      "server_path": "training.contribute_ready",
      "server_key_literal": "\"contribute_ready\"",
      "client_access_expr": "tr.contribute_ready",
      "client_field": "contributeReady",
      "purpose": "Coarse boolean: whether contribution persistence is server-ready."
    },
    {
      "server_path": "training.feedback_persist_enabled",
      "server_key_literal": "\"feedback_persist_enabled\"",
      "client_access_expr": "tr.feedback_persist_enabled",
      "client_field": "feedbackPersistEnabled",
      "purpose": "Coarse boolean: whether durable rating persistence is enabled by server policy."
    },
    {
      "server_path": "tokens.hf_token_type",
      "server_key_literal": "\"hf_token_type\"",
      "client_access_expr": "tk.hf_token_type",
      "client_field": "tokenType",
      "purpose": "Schema-compatible non-secret field emitted as unknown; exact credential class is operator-only diagnostics."
    },
    {
      "server_path": "tokens.hf_dataset_token_type",
      "server_key_literal": "\"hf_dataset_token_type\"",
      "client_access_expr": "tk.hf_dataset_token_type",
      "client_field": "writeTokenType",
      "purpose": "Schema-compatible non-secret field emitted as unknown; exact persistence credential class is operator-only diagnostics."
    },
    {
      "server_path": "tokens.least_privilege_mode",
      "server_key_literal": "\"least_privilege_mode\"",
      "client_access_expr": "tk.least_privilege_mode",
      "client_field": "leastPrivilege",
      "purpose": "Coarse boolean that distinct persistence credentials are configured, without exposing their type/value."
    },
    {
      "server_path": "storage",
      "server_key_literal": "\"storage\"",
      "client_access_expr": "data.storage",
      "client_field": "storage",
      "purpose": "Privacy-minimized summary only: configured, target_count, and primary_ready. No provider/repo/link/token topology."
    }
  ],
  "server_emitted_but_not_yet_consumed": {
    "note": "Detailed routing/token/storage diagnostics are intentionally not emitted by the public endpoint. Add an authenticated operator surface before exposing any such fields.",
    "keys": []
  }
}
