# scikit-plots/ai · direct runtime dependencies · v3.3.0
#
# Human-readable direct dependency manifest.  Production containers install
# requirements.lock with --require-hashes and --only-binary=:all: instead.
# Keep every direct dependency exact; update the lock + SBOM + advisory review
# together rather than widening a range in-place.
#
# SPDX-License-Identifier: BSD-3-Clause

fastapi==0.141.1
python-multipart==0.0.32
uvicorn==0.48.0
httpx==0.28.1
huggingface_hub==1.16.1
redis==8.1.0
