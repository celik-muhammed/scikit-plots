# Logging maintenance

This maintenance plane reviews the package-form `scikitplot.logging`. Use `tools/check_contract.py` as the static gate and the focused/runtime evidence lanes described in `VERIFICATION.md`.
