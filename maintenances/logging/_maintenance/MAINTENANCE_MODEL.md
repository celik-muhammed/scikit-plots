# Maintenance model

Three truths remain separate: maintenance tooling health, runtime structural/behavioral health, and release evidence. The package move must not turn stale single-file fingerprints into authority.

The runtime tree fingerprint covers all non-cache files under `scikitplot/logging/`. Maintenance PASS means only that the maintainer machinery is coherent.
