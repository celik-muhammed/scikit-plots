# MCP maintenance plane

Active maintenance state for `scikitplot.mcp`. Runtime code stays under
`scikitplot/mcp`; maintainer tooling stays here; reusable operating instructions
stay under `skills/mcp/`.

Read `FRESH_CHAT_HANDOFF.md`, then `STATE.json`, `FAMILY.md`, and
`VERIFICATION.md`. Historical M00–M14 campaign artifacts are under `history/`.
