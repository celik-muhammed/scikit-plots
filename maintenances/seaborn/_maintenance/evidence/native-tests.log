..........................FFFFFFF..FFFF.FFFFFF...................FFF.FFF [ 48%]
F.FFF...............................FF.FFFFFFFFFFFFF..................F. [ 96%]
FFFF.F                                                                   [100%]
=================================== FAILURES ===================================
____________________ TestAucplot.test_accepts_numpy_arrays _____________________

self = <scikitplot.seaborn.tests.test__auc.TestAucplot testMethod=test_accepts_numpy_arrays>

    def test_accepts_numpy_arrays(self):
        y_true, y_score = _make_binary()
>       ax = aucplot(x=y_true, y=y_score, kind="roc")
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

scikitplot/seaborn/tests/test__auc.py:331:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
scikitplot/seaborn/_auc.py:1237: in aucplot
    p.plot_aucplot(
scikitplot/seaborn/_auc.py:1079: in plot_aucplot
    artist_kws = self._artist_kws(
scikitplot/seaborn/_auc.py:276: in _artist_kws
    kws.setdefault("color", to_rgba(color, alpha))
                            ^^^^^^^^^^^^^^^^^^^^^
/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:317: in to_rgba
    rgba = _to_rgba_no_colorcycle(c, alpha)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

c = None, alpha = 1.0

    def _to_rgba_no_colorcycle(c, alpha=None):
        """
        Convert *c* to an RGBA color, with no support for color-cycle syntax.

        If *alpha* is given, force the alpha value of the returned RGBA tuple
        to *alpha*. Otherwise, the alpha value from *c* is used, if it has alpha
        information, or defaults to 1.

        *alpha* is ignored for the color value ``"none"`` (case-insensitive),
        which always maps to ``(0, 0, 0, 0)``.
        """
        if alpha is not None and not 0 <= alpha <= 1:
            raise ValueError("'alpha' must be between 0 and 1, inclusive")
        orig_c = c
        if c is np.ma.masked:
            return (0., 0., 0., 0.)
        if isinstance(c, str):
            if c.lower() == "none":
                return (0., 0., 0., 0.)
            # Named color.
            try:
                # This may turn c into a non-string, so we check again below.
                c = _colors_full_map[c]
            except KeyError:
                if len(orig_c) != 1:
                    try:
                        c = _colors_full_map[c.lower()]
                    except KeyError:
                        pass
        if isinstance(c, str):
            # hex color in #rrggbb format.
            match = re.match(r"\A#[a-fA-F0-9]{6}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1:3], c[3:5], c[5:7]])
                        + (alpha if alpha is not None else 1.,))
            # hex color in #rgb format, shorthand for #rrggbb.
            match = re.match(r"\A#[a-fA-F0-9]{3}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1]*2, c[2]*2, c[3]*2])
                        + (alpha if alpha is not None else 1.,))
            # hex color with alpha in #rrggbbaa format.
            match = re.match(r"\A#[a-fA-F0-9]{8}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1:3], c[3:5], c[5:7], c[7:9]]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # hex color with alpha in #rgba format, shorthand for #rrggbbaa.
            match = re.match(r"\A#[a-fA-F0-9]{4}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1]*2, c[2]*2, c[3]*2, c[4]*2]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # string gray.
            try:
                c = float(c)
            except ValueError:
                pass
            else:
                if not (0 <= c <= 1):
                    raise ValueError(
                        f"Invalid string grayscale value {orig_c!r}. "
                        f"Value must be within 0-1 range")
                return c, c, c, alpha if alpha is not None else 1.
            raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
        # turn 2-D array into 1-D array
        if isinstance(c, np.ndarray):
            if c.ndim == 2 and c.shape[0] == 1:
                c = c.reshape(-1)
        # tuple color.
        if not np.iterable(c):
>           raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
E           ValueError: Invalid RGBA argument: None

/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:401: ValueError
____________________ TestAucplot.test_accepts_pandas_series ____________________

self = <scikitplot.seaborn.tests.test__auc.TestAucplot testMethod=test_accepts_pandas_series>

    def test_accepts_pandas_series(self):
        y_true, y_score = _make_binary(100)
>       ax = aucplot(x=pd.Series(y_true), y=pd.Series(y_score), kind="pr")
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

scikitplot/seaborn/tests/test__auc.py:336:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
scikitplot/seaborn/_auc.py:1237: in aucplot
    p.plot_aucplot(
scikitplot/seaborn/_auc.py:1079: in plot_aucplot
    artist_kws = self._artist_kws(
scikitplot/seaborn/_auc.py:276: in _artist_kws
    kws.setdefault("color", to_rgba(color, alpha))
                            ^^^^^^^^^^^^^^^^^^^^^
/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:317: in to_rgba
    rgba = _to_rgba_no_colorcycle(c, alpha)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

c = None, alpha = 1.0

    def _to_rgba_no_colorcycle(c, alpha=None):
        """
        Convert *c* to an RGBA color, with no support for color-cycle syntax.

        If *alpha* is given, force the alpha value of the returned RGBA tuple
        to *alpha*. Otherwise, the alpha value from *c* is used, if it has alpha
        information, or defaults to 1.

        *alpha* is ignored for the color value ``"none"`` (case-insensitive),
        which always maps to ``(0, 0, 0, 0)``.
        """
        if alpha is not None and not 0 <= alpha <= 1:
            raise ValueError("'alpha' must be between 0 and 1, inclusive")
        orig_c = c
        if c is np.ma.masked:
            return (0., 0., 0., 0.)
        if isinstance(c, str):
            if c.lower() == "none":
                return (0., 0., 0., 0.)
            # Named color.
            try:
                # This may turn c into a non-string, so we check again below.
                c = _colors_full_map[c]
            except KeyError:
                if len(orig_c) != 1:
                    try:
                        c = _colors_full_map[c.lower()]
                    except KeyError:
                        pass
        if isinstance(c, str):
            # hex color in #rrggbb format.
            match = re.match(r"\A#[a-fA-F0-9]{6}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1:3], c[3:5], c[5:7]])
                        + (alpha if alpha is not None else 1.,))
            # hex color in #rgb format, shorthand for #rrggbb.
            match = re.match(r"\A#[a-fA-F0-9]{3}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1]*2, c[2]*2, c[3]*2])
                        + (alpha if alpha is not None else 1.,))
            # hex color with alpha in #rrggbbaa format.
            match = re.match(r"\A#[a-fA-F0-9]{8}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1:3], c[3:5], c[5:7], c[7:9]]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # hex color with alpha in #rgba format, shorthand for #rrggbbaa.
            match = re.match(r"\A#[a-fA-F0-9]{4}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1]*2, c[2]*2, c[3]*2, c[4]*2]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # string gray.
            try:
                c = float(c)
            except ValueError:
                pass
            else:
                if not (0 <= c <= 1):
                    raise ValueError(
                        f"Invalid string grayscale value {orig_c!r}. "
                        f"Value must be within 0-1 range")
                return c, c, c, alpha if alpha is not None else 1.
            raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
        # turn 2-D array into 1-D array
        if isinstance(c, np.ndarray):
            if c.ndim == 2 and c.shape[0] == 1:
                c = c.reshape(-1)
        # tuple color.
        if not np.iterable(c):
>           raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
E           ValueError: Invalid RGBA argument: None

/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:401: ValueError
____________________ TestAucplot.test_all_positive_no_crash ____________________

self = <scikitplot.seaborn.tests.test__auc.TestAucplot testMethod=test_all_positive_no_crash>

    def test_all_positive_no_crash(self):
        df = pd.DataFrame({"x": [1] * 50, "y": np.linspace(0.1, 0.9, 50)})
        with warnings.catch_warnings(record=True):
            warnings.simplefilter("always")
>           ax = aucplot(df, x="x", y="y", kind="roc")
                 ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

scikitplot/seaborn/tests/test__auc.py:397:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
scikitplot/seaborn/_auc.py:1237: in aucplot
    p.plot_aucplot(
scikitplot/seaborn/_auc.py:1079: in plot_aucplot
    artist_kws = self._artist_kws(
scikitplot/seaborn/_auc.py:276: in _artist_kws
    kws.setdefault("color", to_rgba(color, alpha))
                            ^^^^^^^^^^^^^^^^^^^^^
/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:317: in to_rgba
    rgba = _to_rgba_no_colorcycle(c, alpha)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

c = None, alpha = 1.0

    def _to_rgba_no_colorcycle(c, alpha=None):
        """
        Convert *c* to an RGBA color, with no support for color-cycle syntax.

        If *alpha* is given, force the alpha value of the returned RGBA tuple
        to *alpha*. Otherwise, the alpha value from *c* is used, if it has alpha
        information, or defaults to 1.

        *alpha* is ignored for the color value ``"none"`` (case-insensitive),
        which always maps to ``(0, 0, 0, 0)``.
        """
        if alpha is not None and not 0 <= alpha <= 1:
            raise ValueError("'alpha' must be between 0 and 1, inclusive")
        orig_c = c
        if c is np.ma.masked:
            return (0., 0., 0., 0.)
        if isinstance(c, str):
            if c.lower() == "none":
                return (0., 0., 0., 0.)
            # Named color.
            try:
                # This may turn c into a non-string, so we check again below.
                c = _colors_full_map[c]
            except KeyError:
                if len(orig_c) != 1:
                    try:
                        c = _colors_full_map[c.lower()]
                    except KeyError:
                        pass
        if isinstance(c, str):
            # hex color in #rrggbb format.
            match = re.match(r"\A#[a-fA-F0-9]{6}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1:3], c[3:5], c[5:7]])
                        + (alpha if alpha is not None else 1.,))
            # hex color in #rgb format, shorthand for #rrggbb.
            match = re.match(r"\A#[a-fA-F0-9]{3}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1]*2, c[2]*2, c[3]*2])
                        + (alpha if alpha is not None else 1.,))
            # hex color with alpha in #rrggbbaa format.
            match = re.match(r"\A#[a-fA-F0-9]{8}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1:3], c[3:5], c[5:7], c[7:9]]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # hex color with alpha in #rgba format, shorthand for #rrggbbaa.
            match = re.match(r"\A#[a-fA-F0-9]{4}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1]*2, c[2]*2, c[3]*2, c[4]*2]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # string gray.
            try:
                c = float(c)
            except ValueError:
                pass
            else:
                if not (0 <= c <= 1):
                    raise ValueError(
                        f"Invalid string grayscale value {orig_c!r}. "
                        f"Value must be within 0-1 range")
                return c, c, c, alpha if alpha is not None else 1.
            raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
        # turn 2-D array into 1-D array
        if isinstance(c, np.ndarray):
            if c.ndim == 2 and c.shape[0] == 1:
                c = c.reshape(-1)
        # tuple color.
        if not np.iterable(c):
>           raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
E           ValueError: Invalid RGBA argument: None

/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:401: ValueError
____________________ TestAucplot.test_auc_in_legend_text_pr ____________________

self = <scikitplot.seaborn.tests.test__auc.TestAucplot testMethod=test_auc_in_legend_text_pr>

    def test_auc_in_legend_text_pr(self):
>       ax = aucplot(_make_df(), x="x", y="y", kind="pr", legend=True)
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

scikitplot/seaborn/tests/test__auc.py:387:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
scikitplot/seaborn/_auc.py:1237: in aucplot
    p.plot_aucplot(
scikitplot/seaborn/_auc.py:1079: in plot_aucplot
    artist_kws = self._artist_kws(
scikitplot/seaborn/_auc.py:276: in _artist_kws
    kws.setdefault("color", to_rgba(color, alpha))
                            ^^^^^^^^^^^^^^^^^^^^^
/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:317: in to_rgba
    rgba = _to_rgba_no_colorcycle(c, alpha)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

c = None, alpha = 1.0

    def _to_rgba_no_colorcycle(c, alpha=None):
        """
        Convert *c* to an RGBA color, with no support for color-cycle syntax.

        If *alpha* is given, force the alpha value of the returned RGBA tuple
        to *alpha*. Otherwise, the alpha value from *c* is used, if it has alpha
        information, or defaults to 1.

        *alpha* is ignored for the color value ``"none"`` (case-insensitive),
        which always maps to ``(0, 0, 0, 0)``.
        """
        if alpha is not None and not 0 <= alpha <= 1:
            raise ValueError("'alpha' must be between 0 and 1, inclusive")
        orig_c = c
        if c is np.ma.masked:
            return (0., 0., 0., 0.)
        if isinstance(c, str):
            if c.lower() == "none":
                return (0., 0., 0., 0.)
            # Named color.
            try:
                # This may turn c into a non-string, so we check again below.
                c = _colors_full_map[c]
            except KeyError:
                if len(orig_c) != 1:
                    try:
                        c = _colors_full_map[c.lower()]
                    except KeyError:
                        pass
        if isinstance(c, str):
            # hex color in #rrggbb format.
            match = re.match(r"\A#[a-fA-F0-9]{6}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1:3], c[3:5], c[5:7]])
                        + (alpha if alpha is not None else 1.,))
            # hex color in #rgb format, shorthand for #rrggbb.
            match = re.match(r"\A#[a-fA-F0-9]{3}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1]*2, c[2]*2, c[3]*2])
                        + (alpha if alpha is not None else 1.,))
            # hex color with alpha in #rrggbbaa format.
            match = re.match(r"\A#[a-fA-F0-9]{8}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1:3], c[3:5], c[5:7], c[7:9]]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # hex color with alpha in #rgba format, shorthand for #rrggbbaa.
            match = re.match(r"\A#[a-fA-F0-9]{4}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1]*2, c[2]*2, c[3]*2, c[4]*2]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # string gray.
            try:
                c = float(c)
            except ValueError:
                pass
            else:
                if not (0 <= c <= 1):
                    raise ValueError(
                        f"Invalid string grayscale value {orig_c!r}. "
                        f"Value must be within 0-1 range")
                return c, c, c, alpha if alpha is not None else 1.
            raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
        # turn 2-D array into 1-D array
        if isinstance(c, np.ndarray):
            if c.ndim == 2 and c.shape[0] == 1:
                c = c.reshape(-1)
        # tuple color.
        if not np.iterable(c):
>           raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
E           ValueError: Invalid RGBA argument: None

/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:401: ValueError
___________________ TestAucplot.test_auc_in_legend_text_roc ____________________

self = <scikitplot.seaborn.tests.test__auc.TestAucplot testMethod=test_auc_in_legend_text_roc>

    def test_auc_in_legend_text_roc(self):
>       ax = aucplot(_make_df(), x="x", y="y", kind="roc", legend=True)
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

scikitplot/seaborn/tests/test__auc.py:380:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
scikitplot/seaborn/_auc.py:1237: in aucplot
    p.plot_aucplot(
scikitplot/seaborn/_auc.py:1079: in plot_aucplot
    artist_kws = self._artist_kws(
scikitplot/seaborn/_auc.py:276: in _artist_kws
    kws.setdefault("color", to_rgba(color, alpha))
                            ^^^^^^^^^^^^^^^^^^^^^
/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:317: in to_rgba
    rgba = _to_rgba_no_colorcycle(c, alpha)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

c = None, alpha = 1.0

    def _to_rgba_no_colorcycle(c, alpha=None):
        """
        Convert *c* to an RGBA color, with no support for color-cycle syntax.

        If *alpha* is given, force the alpha value of the returned RGBA tuple
        to *alpha*. Otherwise, the alpha value from *c* is used, if it has alpha
        information, or defaults to 1.

        *alpha* is ignored for the color value ``"none"`` (case-insensitive),
        which always maps to ``(0, 0, 0, 0)``.
        """
        if alpha is not None and not 0 <= alpha <= 1:
            raise ValueError("'alpha' must be between 0 and 1, inclusive")
        orig_c = c
        if c is np.ma.masked:
            return (0., 0., 0., 0.)
        if isinstance(c, str):
            if c.lower() == "none":
                return (0., 0., 0., 0.)
            # Named color.
            try:
                # This may turn c into a non-string, so we check again below.
                c = _colors_full_map[c]
            except KeyError:
                if len(orig_c) != 1:
                    try:
                        c = _colors_full_map[c.lower()]
                    except KeyError:
                        pass
        if isinstance(c, str):
            # hex color in #rrggbb format.
            match = re.match(r"\A#[a-fA-F0-9]{6}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1:3], c[3:5], c[5:7]])
                        + (alpha if alpha is not None else 1.,))
            # hex color in #rgb format, shorthand for #rrggbb.
            match = re.match(r"\A#[a-fA-F0-9]{3}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1]*2, c[2]*2, c[3]*2])
                        + (alpha if alpha is not None else 1.,))
            # hex color with alpha in #rrggbbaa format.
            match = re.match(r"\A#[a-fA-F0-9]{8}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1:3], c[3:5], c[5:7], c[7:9]]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # hex color with alpha in #rgba format, shorthand for #rrggbbaa.
            match = re.match(r"\A#[a-fA-F0-9]{4}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1]*2, c[2]*2, c[3]*2, c[4]*2]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # string gray.
            try:
                c = float(c)
            except ValueError:
                pass
            else:
                if not (0 <= c <= 1):
                    raise ValueError(
                        f"Invalid string grayscale value {orig_c!r}. "
                        f"Value must be within 0-1 range")
                return c, c, c, alpha if alpha is not None else 1.
            raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
        # turn 2-D array into 1-D array
        if isinstance(c, np.ndarray):
            if c.ndim == 2 and c.shape[0] == 1:
                c = c.reshape(-1)
        # tuple color.
        if not np.iterable(c):
>           raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
E           ValueError: Invalid RGBA argument: None

/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:401: ValueError
_____________________ TestAucplot.test_ax_param_respected ______________________

self = <scikitplot.seaborn.tests.test__auc.TestAucplot testMethod=test_ax_param_respected>

    def test_ax_param_respected(self):
        fig, ax = plt.subplots()
>       result = aucplot(_make_df(), x="x", y="y", kind="roc", ax=ax)
                 ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

scikitplot/seaborn/tests/test__auc.py:372:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
scikitplot/seaborn/_auc.py:1237: in aucplot
    p.plot_aucplot(
scikitplot/seaborn/_auc.py:1079: in plot_aucplot
    artist_kws = self._artist_kws(
scikitplot/seaborn/_auc.py:276: in _artist_kws
    kws.setdefault("color", to_rgba(color, alpha))
                            ^^^^^^^^^^^^^^^^^^^^^
/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:317: in to_rgba
    rgba = _to_rgba_no_colorcycle(c, alpha)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

c = None, alpha = 1.0

    def _to_rgba_no_colorcycle(c, alpha=None):
        """
        Convert *c* to an RGBA color, with no support for color-cycle syntax.

        If *alpha* is given, force the alpha value of the returned RGBA tuple
        to *alpha*. Otherwise, the alpha value from *c* is used, if it has alpha
        information, or defaults to 1.

        *alpha* is ignored for the color value ``"none"`` (case-insensitive),
        which always maps to ``(0, 0, 0, 0)``.
        """
        if alpha is not None and not 0 <= alpha <= 1:
            raise ValueError("'alpha' must be between 0 and 1, inclusive")
        orig_c = c
        if c is np.ma.masked:
            return (0., 0., 0., 0.)
        if isinstance(c, str):
            if c.lower() == "none":
                return (0., 0., 0., 0.)
            # Named color.
            try:
                # This may turn c into a non-string, so we check again below.
                c = _colors_full_map[c]
            except KeyError:
                if len(orig_c) != 1:
                    try:
                        c = _colors_full_map[c.lower()]
                    except KeyError:
                        pass
        if isinstance(c, str):
            # hex color in #rrggbb format.
            match = re.match(r"\A#[a-fA-F0-9]{6}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1:3], c[3:5], c[5:7]])
                        + (alpha if alpha is not None else 1.,))
            # hex color in #rgb format, shorthand for #rrggbb.
            match = re.match(r"\A#[a-fA-F0-9]{3}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1]*2, c[2]*2, c[3]*2])
                        + (alpha if alpha is not None else 1.,))
            # hex color with alpha in #rrggbbaa format.
            match = re.match(r"\A#[a-fA-F0-9]{8}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1:3], c[3:5], c[5:7], c[7:9]]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # hex color with alpha in #rgba format, shorthand for #rrggbbaa.
            match = re.match(r"\A#[a-fA-F0-9]{4}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1]*2, c[2]*2, c[3]*2, c[4]*2]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # string gray.
            try:
                c = float(c)
            except ValueError:
                pass
            else:
                if not (0 <= c <= 1):
                    raise ValueError(
                        f"Invalid string grayscale value {orig_c!r}. "
                        f"Value must be within 0-1 range")
                return c, c, c, alpha if alpha is not None else 1.
            raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
        # turn 2-D array into 1-D array
        if isinstance(c, np.ndarray):
            if c.ndim == 2 and c.shape[0] == 1:
                c = c.reshape(-1)
        # tuple color.
        if not np.iterable(c):
>           raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
E           ValueError: Invalid RGBA argument: None

/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:401: ValueError
__________________ TestAucplot.test_fill_true_does_not_crash ___________________

self = <scikitplot.seaborn.tests.test__auc.TestAucplot testMethod=test_fill_true_does_not_crash>

    def test_fill_true_does_not_crash(self):
>       ax = aucplot(_make_df(), x="x", y="y", kind="pr", fill=True)
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

scikitplot/seaborn/tests/test__auc.py:376:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
scikitplot/seaborn/_auc.py:1237: in aucplot
    p.plot_aucplot(
scikitplot/seaborn/_auc.py:1079: in plot_aucplot
    artist_kws = self._artist_kws(
scikitplot/seaborn/_auc.py:261: in _artist_kws
    kws.setdefault("facecolor", to_rgba(color, alpha))
                                ^^^^^^^^^^^^^^^^^^^^^
/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:317: in to_rgba
    rgba = _to_rgba_no_colorcycle(c, alpha)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

c = None, alpha = 1.0

    def _to_rgba_no_colorcycle(c, alpha=None):
        """
        Convert *c* to an RGBA color, with no support for color-cycle syntax.

        If *alpha* is given, force the alpha value of the returned RGBA tuple
        to *alpha*. Otherwise, the alpha value from *c* is used, if it has alpha
        information, or defaults to 1.

        *alpha* is ignored for the color value ``"none"`` (case-insensitive),
        which always maps to ``(0, 0, 0, 0)``.
        """
        if alpha is not None and not 0 <= alpha <= 1:
            raise ValueError("'alpha' must be between 0 and 1, inclusive")
        orig_c = c
        if c is np.ma.masked:
            return (0., 0., 0., 0.)
        if isinstance(c, str):
            if c.lower() == "none":
                return (0., 0., 0., 0.)
            # Named color.
            try:
                # This may turn c into a non-string, so we check again below.
                c = _colors_full_map[c]
            except KeyError:
                if len(orig_c) != 1:
                    try:
                        c = _colors_full_map[c.lower()]
                    except KeyError:
                        pass
        if isinstance(c, str):
            # hex color in #rrggbb format.
            match = re.match(r"\A#[a-fA-F0-9]{6}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1:3], c[3:5], c[5:7]])
                        + (alpha if alpha is not None else 1.,))
            # hex color in #rgb format, shorthand for #rrggbb.
            match = re.match(r"\A#[a-fA-F0-9]{3}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1]*2, c[2]*2, c[3]*2])
                        + (alpha if alpha is not None else 1.,))
            # hex color with alpha in #rrggbbaa format.
            match = re.match(r"\A#[a-fA-F0-9]{8}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1:3], c[3:5], c[5:7], c[7:9]]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # hex color with alpha in #rgba format, shorthand for #rrggbbaa.
            match = re.match(r"\A#[a-fA-F0-9]{4}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1]*2, c[2]*2, c[3]*2, c[4]*2]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # string gray.
            try:
                c = float(c)
            except ValueError:
                pass
            else:
                if not (0 <= c <= 1):
                    raise ValueError(
                        f"Invalid string grayscale value {orig_c!r}. "
                        f"Value must be within 0-1 range")
                return c, c, c, alpha if alpha is not None else 1.
            raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
        # turn 2-D array into 1-D array
        if isinstance(c, np.ndarray):
            if c.ndim == 2 and c.shape[0] == 1:
                c = c.reshape(-1)
        # tuple color.
        if not np.iterable(c):
>           raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
E           ValueError: Invalid RGBA argument: None

/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:401: ValueError
__________________ TestAucplot.test_kind_none_defaults_to_roc __________________

self = <scikitplot.seaborn.tests.test__auc.TestAucplot testMethod=test_kind_none_defaults_to_roc>

    def test_kind_none_defaults_to_roc(self):
        fig, ax = plt.subplots()
>       result = aucplot(_make_df(), x="x", y="y", ax=ax)
                 ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

scikitplot/seaborn/tests/test__auc.py:312:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
scikitplot/seaborn/_auc.py:1237: in aucplot
    p.plot_aucplot(
scikitplot/seaborn/_auc.py:1079: in plot_aucplot
    artist_kws = self._artist_kws(
scikitplot/seaborn/_auc.py:276: in _artist_kws
    kws.setdefault("color", to_rgba(color, alpha))
                            ^^^^^^^^^^^^^^^^^^^^^
/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:317: in to_rgba
    rgba = _to_rgba_no_colorcycle(c, alpha)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

c = None, alpha = 1.0

    def _to_rgba_no_colorcycle(c, alpha=None):
        """
        Convert *c* to an RGBA color, with no support for color-cycle syntax.

        If *alpha* is given, force the alpha value of the returned RGBA tuple
        to *alpha*. Otherwise, the alpha value from *c* is used, if it has alpha
        information, or defaults to 1.

        *alpha* is ignored for the color value ``"none"`` (case-insensitive),
        which always maps to ``(0, 0, 0, 0)``.
        """
        if alpha is not None and not 0 <= alpha <= 1:
            raise ValueError("'alpha' must be between 0 and 1, inclusive")
        orig_c = c
        if c is np.ma.masked:
            return (0., 0., 0., 0.)
        if isinstance(c, str):
            if c.lower() == "none":
                return (0., 0., 0., 0.)
            # Named color.
            try:
                # This may turn c into a non-string, so we check again below.
                c = _colors_full_map[c]
            except KeyError:
                if len(orig_c) != 1:
                    try:
                        c = _colors_full_map[c.lower()]
                    except KeyError:
                        pass
        if isinstance(c, str):
            # hex color in #rrggbb format.
            match = re.match(r"\A#[a-fA-F0-9]{6}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1:3], c[3:5], c[5:7]])
                        + (alpha if alpha is not None else 1.,))
            # hex color in #rgb format, shorthand for #rrggbb.
            match = re.match(r"\A#[a-fA-F0-9]{3}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1]*2, c[2]*2, c[3]*2])
                        + (alpha if alpha is not None else 1.,))
            # hex color with alpha in #rrggbbaa format.
            match = re.match(r"\A#[a-fA-F0-9]{8}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1:3], c[3:5], c[5:7], c[7:9]]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # hex color with alpha in #rgba format, shorthand for #rrggbbaa.
            match = re.match(r"\A#[a-fA-F0-9]{4}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1]*2, c[2]*2, c[3]*2, c[4]*2]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # string gray.
            try:
                c = float(c)
            except ValueError:
                pass
            else:
                if not (0 <= c <= 1):
                    raise ValueError(
                        f"Invalid string grayscale value {orig_c!r}. "
                        f"Value must be within 0-1 range")
                return c, c, c, alpha if alpha is not None else 1.
            raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
        # turn 2-D array into 1-D array
        if isinstance(c, np.ndarray):
            if c.ndim == 2 and c.shape[0] == 1:
                c = c.reshape(-1)
        # tuple color.
        if not np.iterable(c):
>           raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
E           ValueError: Invalid RGBA argument: None

/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:401: ValueError
___________________ TestAucplot.test_kind_uppercase_accepted ___________________

self = <scikitplot.seaborn.tests.test__auc.TestAucplot testMethod=test_kind_uppercase_accepted>

    def test_kind_uppercase_accepted(self):
>       ax = aucplot(_make_df(), x="x", y="y", kind="ROC")
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

scikitplot/seaborn/tests/test__auc.py:316:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
scikitplot/seaborn/_auc.py:1237: in aucplot
    p.plot_aucplot(
scikitplot/seaborn/_auc.py:1079: in plot_aucplot
    artist_kws = self._artist_kws(
scikitplot/seaborn/_auc.py:276: in _artist_kws
    kws.setdefault("color", to_rgba(color, alpha))
                            ^^^^^^^^^^^^^^^^^^^^^
/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:317: in to_rgba
    rgba = _to_rgba_no_colorcycle(c, alpha)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

c = None, alpha = 1.0

    def _to_rgba_no_colorcycle(c, alpha=None):
        """
        Convert *c* to an RGBA color, with no support for color-cycle syntax.

        If *alpha* is given, force the alpha value of the returned RGBA tuple
        to *alpha*. Otherwise, the alpha value from *c* is used, if it has alpha
        information, or defaults to 1.

        *alpha* is ignored for the color value ``"none"`` (case-insensitive),
        which always maps to ``(0, 0, 0, 0)``.
        """
        if alpha is not None and not 0 <= alpha <= 1:
            raise ValueError("'alpha' must be between 0 and 1, inclusive")
        orig_c = c
        if c is np.ma.masked:
            return (0., 0., 0., 0.)
        if isinstance(c, str):
            if c.lower() == "none":
                return (0., 0., 0., 0.)
            # Named color.
            try:
                # This may turn c into a non-string, so we check again below.
                c = _colors_full_map[c]
            except KeyError:
                if len(orig_c) != 1:
                    try:
                        c = _colors_full_map[c.lower()]
                    except KeyError:
                        pass
        if isinstance(c, str):
            # hex color in #rrggbb format.
            match = re.match(r"\A#[a-fA-F0-9]{6}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1:3], c[3:5], c[5:7]])
                        + (alpha if alpha is not None else 1.,))
            # hex color in #rgb format, shorthand for #rrggbb.
            match = re.match(r"\A#[a-fA-F0-9]{3}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1]*2, c[2]*2, c[3]*2])
                        + (alpha if alpha is not None else 1.,))
            # hex color with alpha in #rrggbbaa format.
            match = re.match(r"\A#[a-fA-F0-9]{8}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1:3], c[3:5], c[5:7], c[7:9]]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # hex color with alpha in #rgba format, shorthand for #rrggbbaa.
            match = re.match(r"\A#[a-fA-F0-9]{4}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1]*2, c[2]*2, c[3]*2, c[4]*2]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # string gray.
            try:
                c = float(c)
            except ValueError:
                pass
            else:
                if not (0 <= c <= 1):
                    raise ValueError(
                        f"Invalid string grayscale value {orig_c!r}. "
                        f"Value must be within 0-1 range")
                return c, c, c, alpha if alpha is not None else 1.
            raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
        # turn 2-D array into 1-D array
        if isinstance(c, np.ndarray):
            if c.ndim == 2 and c.shape[0] == 1:
                c = c.reshape(-1)
        # tuple color.
        if not np.iterable(c):
>           raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
E           ValueError: Invalid RGBA argument: None

/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:401: ValueError
___________________ TestAucplot.test_legend_false_no_legend ____________________

self = <scikitplot.seaborn.tests.test__auc.TestAucplot testMethod=test_legend_false_no_legend>

    def test_legend_false_no_legend(self):
>       ax = aucplot(_make_df(), x="x", y="y", kind="roc", legend=False)
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

scikitplot/seaborn/tests/test__auc.py:367:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
scikitplot/seaborn/_auc.py:1237: in aucplot
    p.plot_aucplot(
scikitplot/seaborn/_auc.py:1079: in plot_aucplot
    artist_kws = self._artist_kws(
scikitplot/seaborn/_auc.py:276: in _artist_kws
    kws.setdefault("color", to_rgba(color, alpha))
                            ^^^^^^^^^^^^^^^^^^^^^
/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:317: in to_rgba
    rgba = _to_rgba_no_colorcycle(c, alpha)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

c = None, alpha = 1.0

    def _to_rgba_no_colorcycle(c, alpha=None):
        """
        Convert *c* to an RGBA color, with no support for color-cycle syntax.

        If *alpha* is given, force the alpha value of the returned RGBA tuple
        to *alpha*. Otherwise, the alpha value from *c* is used, if it has alpha
        information, or defaults to 1.

        *alpha* is ignored for the color value ``"none"`` (case-insensitive),
        which always maps to ``(0, 0, 0, 0)``.
        """
        if alpha is not None and not 0 <= alpha <= 1:
            raise ValueError("'alpha' must be between 0 and 1, inclusive")
        orig_c = c
        if c is np.ma.masked:
            return (0., 0., 0., 0.)
        if isinstance(c, str):
            if c.lower() == "none":
                return (0., 0., 0., 0.)
            # Named color.
            try:
                # This may turn c into a non-string, so we check again below.
                c = _colors_full_map[c]
            except KeyError:
                if len(orig_c) != 1:
                    try:
                        c = _colors_full_map[c.lower()]
                    except KeyError:
                        pass
        if isinstance(c, str):
            # hex color in #rrggbb format.
            match = re.match(r"\A#[a-fA-F0-9]{6}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1:3], c[3:5], c[5:7]])
                        + (alpha if alpha is not None else 1.,))
            # hex color in #rgb format, shorthand for #rrggbb.
            match = re.match(r"\A#[a-fA-F0-9]{3}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1]*2, c[2]*2, c[3]*2])
                        + (alpha if alpha is not None else 1.,))
            # hex color with alpha in #rrggbbaa format.
            match = re.match(r"\A#[a-fA-F0-9]{8}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1:3], c[3:5], c[5:7], c[7:9]]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # hex color with alpha in #rgba format, shorthand for #rrggbbaa.
            match = re.match(r"\A#[a-fA-F0-9]{4}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1]*2, c[2]*2, c[3]*2, c[4]*2]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # string gray.
            try:
                c = float(c)
            except ValueError:
                pass
            else:
                if not (0 <= c <= 1):
                    raise ValueError(
                        f"Invalid string grayscale value {orig_c!r}. "
                        f"Value must be within 0-1 range")
                return c, c, c, alpha if alpha is not None else 1.
            raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
        # turn 2-D array into 1-D array
        if isinstance(c, np.ndarray):
            if c.ndim == 2 and c.shape[0] == 1:
                c = c.reshape(-1)
        # tuple color.
        if not np.iterable(c):
>           raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
E           ValueError: Invalid RGBA argument: None

/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:401: ValueError
_____________________ TestAucplot.test_minimum_two_samples _____________________

self = <scikitplot.seaborn.tests.test__auc.TestAucplot testMethod=test_minimum_two_samples>

    def test_minimum_two_samples(self):
        df = pd.DataFrame({"x": [0, 1], "y": [0.2, 0.8]})
>       ax = aucplot(df, x="x", y="y", kind="roc")
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

scikitplot/seaborn/tests/test__auc.py:402:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
scikitplot/seaborn/_auc.py:1237: in aucplot
    p.plot_aucplot(
scikitplot/seaborn/_auc.py:1079: in plot_aucplot
    artist_kws = self._artist_kws(
scikitplot/seaborn/_auc.py:276: in _artist_kws
    kws.setdefault("color", to_rgba(color, alpha))
                            ^^^^^^^^^^^^^^^^^^^^^
/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:317: in to_rgba
    rgba = _to_rgba_no_colorcycle(c, alpha)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

c = None, alpha = 1.0

    def _to_rgba_no_colorcycle(c, alpha=None):
        """
        Convert *c* to an RGBA color, with no support for color-cycle syntax.

        If *alpha* is given, force the alpha value of the returned RGBA tuple
        to *alpha*. Otherwise, the alpha value from *c* is used, if it has alpha
        information, or defaults to 1.

        *alpha* is ignored for the color value ``"none"`` (case-insensitive),
        which always maps to ``(0, 0, 0, 0)``.
        """
        if alpha is not None and not 0 <= alpha <= 1:
            raise ValueError("'alpha' must be between 0 and 1, inclusive")
        orig_c = c
        if c is np.ma.masked:
            return (0., 0., 0., 0.)
        if isinstance(c, str):
            if c.lower() == "none":
                return (0., 0., 0., 0.)
            # Named color.
            try:
                # This may turn c into a non-string, so we check again below.
                c = _colors_full_map[c]
            except KeyError:
                if len(orig_c) != 1:
                    try:
                        c = _colors_full_map[c.lower()]
                    except KeyError:
                        pass
        if isinstance(c, str):
            # hex color in #rrggbb format.
            match = re.match(r"\A#[a-fA-F0-9]{6}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1:3], c[3:5], c[5:7]])
                        + (alpha if alpha is not None else 1.,))
            # hex color in #rgb format, shorthand for #rrggbb.
            match = re.match(r"\A#[a-fA-F0-9]{3}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1]*2, c[2]*2, c[3]*2])
                        + (alpha if alpha is not None else 1.,))
            # hex color with alpha in #rrggbbaa format.
            match = re.match(r"\A#[a-fA-F0-9]{8}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1:3], c[3:5], c[5:7], c[7:9]]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # hex color with alpha in #rgba format, shorthand for #rrggbbaa.
            match = re.match(r"\A#[a-fA-F0-9]{4}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1]*2, c[2]*2, c[3]*2, c[4]*2]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # string gray.
            try:
                c = float(c)
            except ValueError:
                pass
            else:
                if not (0 <= c <= 1):
                    raise ValueError(
                        f"Invalid string grayscale value {orig_c!r}. "
                        f"Value must be within 0-1 range")
                return c, c, c, alpha if alpha is not None else 1.
            raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
        # turn 2-D array into 1-D array
        if isinstance(c, np.ndarray):
            if c.ndim == 2 and c.shape[0] == 1:
                c = c.reshape(-1)
        # tuple color.
        if not np.iterable(c):
>           raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
E           ValueError: Invalid RGBA argument: None

/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:401: ValueError
__________________ TestAucplot.test_pr_xlabel_contains_recall __________________

self = <scikitplot.seaborn.tests.test__auc.TestAucplot testMethod=test_pr_xlabel_contains_recall>

    def test_pr_xlabel_contains_recall(self):
>       ax = aucplot(_make_df(), x="x", y="y", kind="pr")
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

scikitplot/seaborn/tests/test__auc.py:348:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
scikitplot/seaborn/_auc.py:1237: in aucplot
    p.plot_aucplot(
scikitplot/seaborn/_auc.py:1079: in plot_aucplot
    artist_kws = self._artist_kws(
scikitplot/seaborn/_auc.py:276: in _artist_kws
    kws.setdefault("color", to_rgba(color, alpha))
                            ^^^^^^^^^^^^^^^^^^^^^
/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:317: in to_rgba
    rgba = _to_rgba_no_colorcycle(c, alpha)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

c = None, alpha = 1.0

    def _to_rgba_no_colorcycle(c, alpha=None):
        """
        Convert *c* to an RGBA color, with no support for color-cycle syntax.

        If *alpha* is given, force the alpha value of the returned RGBA tuple
        to *alpha*. Otherwise, the alpha value from *c* is used, if it has alpha
        information, or defaults to 1.

        *alpha* is ignored for the color value ``"none"`` (case-insensitive),
        which always maps to ``(0, 0, 0, 0)``.
        """
        if alpha is not None and not 0 <= alpha <= 1:
            raise ValueError("'alpha' must be between 0 and 1, inclusive")
        orig_c = c
        if c is np.ma.masked:
            return (0., 0., 0., 0.)
        if isinstance(c, str):
            if c.lower() == "none":
                return (0., 0., 0., 0.)
            # Named color.
            try:
                # This may turn c into a non-string, so we check again below.
                c = _colors_full_map[c]
            except KeyError:
                if len(orig_c) != 1:
                    try:
                        c = _colors_full_map[c.lower()]
                    except KeyError:
                        pass
        if isinstance(c, str):
            # hex color in #rrggbb format.
            match = re.match(r"\A#[a-fA-F0-9]{6}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1:3], c[3:5], c[5:7]])
                        + (alpha if alpha is not None else 1.,))
            # hex color in #rgb format, shorthand for #rrggbb.
            match = re.match(r"\A#[a-fA-F0-9]{3}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1]*2, c[2]*2, c[3]*2])
                        + (alpha if alpha is not None else 1.,))
            # hex color with alpha in #rrggbbaa format.
            match = re.match(r"\A#[a-fA-F0-9]{8}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1:3], c[3:5], c[5:7], c[7:9]]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # hex color with alpha in #rgba format, shorthand for #rrggbbaa.
            match = re.match(r"\A#[a-fA-F0-9]{4}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1]*2, c[2]*2, c[3]*2, c[4]*2]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # string gray.
            try:
                c = float(c)
            except ValueError:
                pass
            else:
                if not (0 <= c <= 1):
                    raise ValueError(
                        f"Invalid string grayscale value {orig_c!r}. "
                        f"Value must be within 0-1 range")
                return c, c, c, alpha if alpha is not None else 1.
            raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
        # turn 2-D array into 1-D array
        if isinstance(c, np.ndarray):
            if c.ndim == 2 and c.shape[0] == 1:
                c = c.reshape(-1)
        # tuple color.
        if not np.iterable(c):
>           raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
E           ValueError: Invalid RGBA argument: None

/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:401: ValueError
________________ TestAucplot.test_pr_ylabel_contains_precision _________________

self = <scikitplot.seaborn.tests.test__auc.TestAucplot testMethod=test_pr_ylabel_contains_precision>

    def test_pr_ylabel_contains_precision(self):
>       ax = aucplot(_make_df(), x="x", y="y", kind="pr")
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

scikitplot/seaborn/tests/test__auc.py:352:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
scikitplot/seaborn/_auc.py:1237: in aucplot
    p.plot_aucplot(
scikitplot/seaborn/_auc.py:1079: in plot_aucplot
    artist_kws = self._artist_kws(
scikitplot/seaborn/_auc.py:276: in _artist_kws
    kws.setdefault("color", to_rgba(color, alpha))
                            ^^^^^^^^^^^^^^^^^^^^^
/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:317: in to_rgba
    rgba = _to_rgba_no_colorcycle(c, alpha)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

c = None, alpha = 1.0

    def _to_rgba_no_colorcycle(c, alpha=None):
        """
        Convert *c* to an RGBA color, with no support for color-cycle syntax.

        If *alpha* is given, force the alpha value of the returned RGBA tuple
        to *alpha*. Otherwise, the alpha value from *c* is used, if it has alpha
        information, or defaults to 1.

        *alpha* is ignored for the color value ``"none"`` (case-insensitive),
        which always maps to ``(0, 0, 0, 0)``.
        """
        if alpha is not None and not 0 <= alpha <= 1:
            raise ValueError("'alpha' must be between 0 and 1, inclusive")
        orig_c = c
        if c is np.ma.masked:
            return (0., 0., 0., 0.)
        if isinstance(c, str):
            if c.lower() == "none":
                return (0., 0., 0., 0.)
            # Named color.
            try:
                # This may turn c into a non-string, so we check again below.
                c = _colors_full_map[c]
            except KeyError:
                if len(orig_c) != 1:
                    try:
                        c = _colors_full_map[c.lower()]
                    except KeyError:
                        pass
        if isinstance(c, str):
            # hex color in #rrggbb format.
            match = re.match(r"\A#[a-fA-F0-9]{6}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1:3], c[3:5], c[5:7]])
                        + (alpha if alpha is not None else 1.,))
            # hex color in #rgb format, shorthand for #rrggbb.
            match = re.match(r"\A#[a-fA-F0-9]{3}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1]*2, c[2]*2, c[3]*2])
                        + (alpha if alpha is not None else 1.,))
            # hex color with alpha in #rrggbbaa format.
            match = re.match(r"\A#[a-fA-F0-9]{8}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1:3], c[3:5], c[5:7], c[7:9]]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # hex color with alpha in #rgba format, shorthand for #rrggbbaa.
            match = re.match(r"\A#[a-fA-F0-9]{4}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1]*2, c[2]*2, c[3]*2, c[4]*2]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # string gray.
            try:
                c = float(c)
            except ValueError:
                pass
            else:
                if not (0 <= c <= 1):
                    raise ValueError(
                        f"Invalid string grayscale value {orig_c!r}. "
                        f"Value must be within 0-1 range")
                return c, c, c, alpha if alpha is not None else 1.
            raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
        # turn 2-D array into 1-D array
        if isinstance(c, np.ndarray):
            if c.ndim == 2 and c.shape[0] == 1:
                c = c.reshape(-1)
        # tuple color.
        if not np.iterable(c):
>           raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
E           ValueError: Invalid RGBA argument: None

/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:401: ValueError
_______________________ TestAucplot.test_returns_axes_pr _______________________

self = <scikitplot.seaborn.tests.test__auc.TestAucplot testMethod=test_returns_axes_pr>

    def test_returns_axes_pr(self):
>       ax = aucplot(_make_df(), x="x", y="y", kind="pr")
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

scikitplot/seaborn/tests/test__auc.py:307:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
scikitplot/seaborn/_auc.py:1237: in aucplot
    p.plot_aucplot(
scikitplot/seaborn/_auc.py:1079: in plot_aucplot
    artist_kws = self._artist_kws(
scikitplot/seaborn/_auc.py:276: in _artist_kws
    kws.setdefault("color", to_rgba(color, alpha))
                            ^^^^^^^^^^^^^^^^^^^^^
/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:317: in to_rgba
    rgba = _to_rgba_no_colorcycle(c, alpha)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

c = None, alpha = 1.0

    def _to_rgba_no_colorcycle(c, alpha=None):
        """
        Convert *c* to an RGBA color, with no support for color-cycle syntax.

        If *alpha* is given, force the alpha value of the returned RGBA tuple
        to *alpha*. Otherwise, the alpha value from *c* is used, if it has alpha
        information, or defaults to 1.

        *alpha* is ignored for the color value ``"none"`` (case-insensitive),
        which always maps to ``(0, 0, 0, 0)``.
        """
        if alpha is not None and not 0 <= alpha <= 1:
            raise ValueError("'alpha' must be between 0 and 1, inclusive")
        orig_c = c
        if c is np.ma.masked:
            return (0., 0., 0., 0.)
        if isinstance(c, str):
            if c.lower() == "none":
                return (0., 0., 0., 0.)
            # Named color.
            try:
                # This may turn c into a non-string, so we check again below.
                c = _colors_full_map[c]
            except KeyError:
                if len(orig_c) != 1:
                    try:
                        c = _colors_full_map[c.lower()]
                    except KeyError:
                        pass
        if isinstance(c, str):
            # hex color in #rrggbb format.
            match = re.match(r"\A#[a-fA-F0-9]{6}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1:3], c[3:5], c[5:7]])
                        + (alpha if alpha is not None else 1.,))
            # hex color in #rgb format, shorthand for #rrggbb.
            match = re.match(r"\A#[a-fA-F0-9]{3}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1]*2, c[2]*2, c[3]*2])
                        + (alpha if alpha is not None else 1.,))
            # hex color with alpha in #rrggbbaa format.
            match = re.match(r"\A#[a-fA-F0-9]{8}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1:3], c[3:5], c[5:7], c[7:9]]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # hex color with alpha in #rgba format, shorthand for #rrggbbaa.
            match = re.match(r"\A#[a-fA-F0-9]{4}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1]*2, c[2]*2, c[3]*2, c[4]*2]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # string gray.
            try:
                c = float(c)
            except ValueError:
                pass
            else:
                if not (0 <= c <= 1):
                    raise ValueError(
                        f"Invalid string grayscale value {orig_c!r}. "
                        f"Value must be within 0-1 range")
                return c, c, c, alpha if alpha is not None else 1.
            raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
        # turn 2-D array into 1-D array
        if isinstance(c, np.ndarray):
            if c.ndim == 2 and c.shape[0] == 1:
                c = c.reshape(-1)
        # tuple color.
        if not np.iterable(c):
>           raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
E           ValueError: Invalid RGBA argument: None

/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:401: ValueError
______________________ TestAucplot.test_returns_axes_roc _______________________

self = <scikitplot.seaborn.tests.test__auc.TestAucplot testMethod=test_returns_axes_roc>

    def test_returns_axes_roc(self):
>       ax = aucplot(_make_df(), x="x", y="y", kind="roc")
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

scikitplot/seaborn/tests/test__auc.py:303:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
scikitplot/seaborn/_auc.py:1237: in aucplot
    p.plot_aucplot(
scikitplot/seaborn/_auc.py:1079: in plot_aucplot
    artist_kws = self._artist_kws(
scikitplot/seaborn/_auc.py:276: in _artist_kws
    kws.setdefault("color", to_rgba(color, alpha))
                            ^^^^^^^^^^^^^^^^^^^^^
/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:317: in to_rgba
    rgba = _to_rgba_no_colorcycle(c, alpha)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

c = None, alpha = 1.0

    def _to_rgba_no_colorcycle(c, alpha=None):
        """
        Convert *c* to an RGBA color, with no support for color-cycle syntax.

        If *alpha* is given, force the alpha value of the returned RGBA tuple
        to *alpha*. Otherwise, the alpha value from *c* is used, if it has alpha
        information, or defaults to 1.

        *alpha* is ignored for the color value ``"none"`` (case-insensitive),
        which always maps to ``(0, 0, 0, 0)``.
        """
        if alpha is not None and not 0 <= alpha <= 1:
            raise ValueError("'alpha' must be between 0 and 1, inclusive")
        orig_c = c
        if c is np.ma.masked:
            return (0., 0., 0., 0.)
        if isinstance(c, str):
            if c.lower() == "none":
                return (0., 0., 0., 0.)
            # Named color.
            try:
                # This may turn c into a non-string, so we check again below.
                c = _colors_full_map[c]
            except KeyError:
                if len(orig_c) != 1:
                    try:
                        c = _colors_full_map[c.lower()]
                    except KeyError:
                        pass
        if isinstance(c, str):
            # hex color in #rrggbb format.
            match = re.match(r"\A#[a-fA-F0-9]{6}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1:3], c[3:5], c[5:7]])
                        + (alpha if alpha is not None else 1.,))
            # hex color in #rgb format, shorthand for #rrggbb.
            match = re.match(r"\A#[a-fA-F0-9]{3}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1]*2, c[2]*2, c[3]*2])
                        + (alpha if alpha is not None else 1.,))
            # hex color with alpha in #rrggbbaa format.
            match = re.match(r"\A#[a-fA-F0-9]{8}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1:3], c[3:5], c[5:7], c[7:9]]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # hex color with alpha in #rgba format, shorthand for #rrggbbaa.
            match = re.match(r"\A#[a-fA-F0-9]{4}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1]*2, c[2]*2, c[3]*2, c[4]*2]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # string gray.
            try:
                c = float(c)
            except ValueError:
                pass
            else:
                if not (0 <= c <= 1):
                    raise ValueError(
                        f"Invalid string grayscale value {orig_c!r}. "
                        f"Value must be within 0-1 range")
                return c, c, c, alpha if alpha is not None else 1.
            raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
        # turn 2-D array into 1-D array
        if isinstance(c, np.ndarray):
            if c.ndim == 2 and c.shape[0] == 1:
                c = c.reshape(-1)
        # tuple color.
        if not np.iterable(c):
>           raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
E           ValueError: Invalid RGBA argument: None

/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:401: ValueError
___________________ TestAucplot.test_roc_xlabel_contains_fpr ___________________

self = <scikitplot.seaborn.tests.test__auc.TestAucplot testMethod=test_roc_xlabel_contains_fpr>

    def test_roc_xlabel_contains_fpr(self):
>       ax = aucplot(_make_df(), x="x", y="y", kind="roc")
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

scikitplot/seaborn/tests/test__auc.py:340:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
scikitplot/seaborn/_auc.py:1237: in aucplot
    p.plot_aucplot(
scikitplot/seaborn/_auc.py:1079: in plot_aucplot
    artist_kws = self._artist_kws(
scikitplot/seaborn/_auc.py:276: in _artist_kws
    kws.setdefault("color", to_rgba(color, alpha))
                            ^^^^^^^^^^^^^^^^^^^^^
/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:317: in to_rgba
    rgba = _to_rgba_no_colorcycle(c, alpha)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

c = None, alpha = 1.0

    def _to_rgba_no_colorcycle(c, alpha=None):
        """
        Convert *c* to an RGBA color, with no support for color-cycle syntax.

        If *alpha* is given, force the alpha value of the returned RGBA tuple
        to *alpha*. Otherwise, the alpha value from *c* is used, if it has alpha
        information, or defaults to 1.

        *alpha* is ignored for the color value ``"none"`` (case-insensitive),
        which always maps to ``(0, 0, 0, 0)``.
        """
        if alpha is not None and not 0 <= alpha <= 1:
            raise ValueError("'alpha' must be between 0 and 1, inclusive")
        orig_c = c
        if c is np.ma.masked:
            return (0., 0., 0., 0.)
        if isinstance(c, str):
            if c.lower() == "none":
                return (0., 0., 0., 0.)
            # Named color.
            try:
                # This may turn c into a non-string, so we check again below.
                c = _colors_full_map[c]
            except KeyError:
                if len(orig_c) != 1:
                    try:
                        c = _colors_full_map[c.lower()]
                    except KeyError:
                        pass
        if isinstance(c, str):
            # hex color in #rrggbb format.
            match = re.match(r"\A#[a-fA-F0-9]{6}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1:3], c[3:5], c[5:7]])
                        + (alpha if alpha is not None else 1.,))
            # hex color in #rgb format, shorthand for #rrggbb.
            match = re.match(r"\A#[a-fA-F0-9]{3}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1]*2, c[2]*2, c[3]*2])
                        + (alpha if alpha is not None else 1.,))
            # hex color with alpha in #rrggbbaa format.
            match = re.match(r"\A#[a-fA-F0-9]{8}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1:3], c[3:5], c[5:7], c[7:9]]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # hex color with alpha in #rgba format, shorthand for #rrggbbaa.
            match = re.match(r"\A#[a-fA-F0-9]{4}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1]*2, c[2]*2, c[3]*2, c[4]*2]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # string gray.
            try:
                c = float(c)
            except ValueError:
                pass
            else:
                if not (0 <= c <= 1):
                    raise ValueError(
                        f"Invalid string grayscale value {orig_c!r}. "
                        f"Value must be within 0-1 range")
                return c, c, c, alpha if alpha is not None else 1.
            raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
        # turn 2-D array into 1-D array
        if isinstance(c, np.ndarray):
            if c.ndim == 2 and c.shape[0] == 1:
                c = c.reshape(-1)
        # tuple color.
        if not np.iterable(c):
>           raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
E           ValueError: Invalid RGBA argument: None

/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:401: ValueError
___________________ TestAucplot.test_roc_ylabel_contains_tpr ___________________

self = <scikitplot.seaborn.tests.test__auc.TestAucplot testMethod=test_roc_ylabel_contains_tpr>

    def test_roc_ylabel_contains_tpr(self):
>       ax = aucplot(_make_df(), x="x", y="y", kind="roc")
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

scikitplot/seaborn/tests/test__auc.py:344:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
scikitplot/seaborn/_auc.py:1237: in aucplot
    p.plot_aucplot(
scikitplot/seaborn/_auc.py:1079: in plot_aucplot
    artist_kws = self._artist_kws(
scikitplot/seaborn/_auc.py:276: in _artist_kws
    kws.setdefault("color", to_rgba(color, alpha))
                            ^^^^^^^^^^^^^^^^^^^^^
/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:317: in to_rgba
    rgba = _to_rgba_no_colorcycle(c, alpha)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

c = None, alpha = 1.0

    def _to_rgba_no_colorcycle(c, alpha=None):
        """
        Convert *c* to an RGBA color, with no support for color-cycle syntax.

        If *alpha* is given, force the alpha value of the returned RGBA tuple
        to *alpha*. Otherwise, the alpha value from *c* is used, if it has alpha
        information, or defaults to 1.

        *alpha* is ignored for the color value ``"none"`` (case-insensitive),
        which always maps to ``(0, 0, 0, 0)``.
        """
        if alpha is not None and not 0 <= alpha <= 1:
            raise ValueError("'alpha' must be between 0 and 1, inclusive")
        orig_c = c
        if c is np.ma.masked:
            return (0., 0., 0., 0.)
        if isinstance(c, str):
            if c.lower() == "none":
                return (0., 0., 0., 0.)
            # Named color.
            try:
                # This may turn c into a non-string, so we check again below.
                c = _colors_full_map[c]
            except KeyError:
                if len(orig_c) != 1:
                    try:
                        c = _colors_full_map[c.lower()]
                    except KeyError:
                        pass
        if isinstance(c, str):
            # hex color in #rrggbb format.
            match = re.match(r"\A#[a-fA-F0-9]{6}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1:3], c[3:5], c[5:7]])
                        + (alpha if alpha is not None else 1.,))
            # hex color in #rgb format, shorthand for #rrggbb.
            match = re.match(r"\A#[a-fA-F0-9]{3}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1]*2, c[2]*2, c[3]*2])
                        + (alpha if alpha is not None else 1.,))
            # hex color with alpha in #rrggbbaa format.
            match = re.match(r"\A#[a-fA-F0-9]{8}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1:3], c[3:5], c[5:7], c[7:9]]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # hex color with alpha in #rgba format, shorthand for #rrggbbaa.
            match = re.match(r"\A#[a-fA-F0-9]{4}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1]*2, c[2]*2, c[3]*2, c[4]*2]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # string gray.
            try:
                c = float(c)
            except ValueError:
                pass
            else:
                if not (0 <= c <= 1):
                    raise ValueError(
                        f"Invalid string grayscale value {orig_c!r}. "
                        f"Value must be within 0-1 range")
                return c, c, c, alpha if alpha is not None else 1.
            raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
        # turn 2-D array into 1-D array
        if isinstance(c, np.ndarray):
            if c.ndim == 2 and c.shape[0] == 1:
                c = c.reshape(-1)
        # tuple color.
        if not np.iterable(c):
>           raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
E           ValueError: Invalid RGBA argument: None

/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:401: ValueError
_____________________ TestEvalplot.test_allow_probs_binary _____________________

self = <scikitplot.seaborn.tests.test__confusion_matrix.TestEvalplot testMethod=test_allow_probs_binary>

    def test_allow_probs_binary(self):
        rng = np.random.default_rng(1)
        n = 100
        df = pd.DataFrame({
            "x": rng.integers(0, 2, n),
            "y": np.clip(rng.uniform(0, 1, n), 0, 1),
        })
>       ax = evalplot(df, x="x", y="y", kind="confusion_matrix", allow_probs=True)
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

scikitplot/seaborn/tests/test__confusion_matrix.py:270:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
scikitplot/seaborn/_confusion_matrix.py:1195: in evalplot
    p.plot_evalplot(
scikitplot/seaborn/_confusion_matrix.py:1032: in plot_evalplot
    artist_kws = self._artist_kws(
scikitplot/seaborn/_confusion_matrix.py:260: in _artist_kws
    kws["color"] = to_rgba(color, alpha)
                   ^^^^^^^^^^^^^^^^^^^^^
/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:317: in to_rgba
    rgba = _to_rgba_no_colorcycle(c, alpha)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

c = None, alpha = 1.0

    def _to_rgba_no_colorcycle(c, alpha=None):
        """
        Convert *c* to an RGBA color, with no support for color-cycle syntax.

        If *alpha* is given, force the alpha value of the returned RGBA tuple
        to *alpha*. Otherwise, the alpha value from *c* is used, if it has alpha
        information, or defaults to 1.

        *alpha* is ignored for the color value ``"none"`` (case-insensitive),
        which always maps to ``(0, 0, 0, 0)``.
        """
        if alpha is not None and not 0 <= alpha <= 1:
            raise ValueError("'alpha' must be between 0 and 1, inclusive")
        orig_c = c
        if c is np.ma.masked:
            return (0., 0., 0., 0.)
        if isinstance(c, str):
            if c.lower() == "none":
                return (0., 0., 0., 0.)
            # Named color.
            try:
                # This may turn c into a non-string, so we check again below.
                c = _colors_full_map[c]
            except KeyError:
                if len(orig_c) != 1:
                    try:
                        c = _colors_full_map[c.lower()]
                    except KeyError:
                        pass
        if isinstance(c, str):
            # hex color in #rrggbb format.
            match = re.match(r"\A#[a-fA-F0-9]{6}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1:3], c[3:5], c[5:7]])
                        + (alpha if alpha is not None else 1.,))
            # hex color in #rgb format, shorthand for #rrggbb.
            match = re.match(r"\A#[a-fA-F0-9]{3}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1]*2, c[2]*2, c[3]*2])
                        + (alpha if alpha is not None else 1.,))
            # hex color with alpha in #rrggbbaa format.
            match = re.match(r"\A#[a-fA-F0-9]{8}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1:3], c[3:5], c[5:7], c[7:9]]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # hex color with alpha in #rgba format, shorthand for #rrggbbaa.
            match = re.match(r"\A#[a-fA-F0-9]{4}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1]*2, c[2]*2, c[3]*2, c[4]*2]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # string gray.
            try:
                c = float(c)
            except ValueError:
                pass
            else:
                if not (0 <= c <= 1):
                    raise ValueError(
                        f"Invalid string grayscale value {orig_c!r}. "
                        f"Value must be within 0-1 range")
                return c, c, c, alpha if alpha is not None else 1.
            raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
        # turn 2-D array into 1-D array
        if isinstance(c, np.ndarray):
            if c.ndim == 2 and c.shape[0] == 1:
                c = c.reshape(-1)
        # tuple color.
        if not np.iterable(c):
>           raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
E           ValueError: Invalid RGBA argument: None

/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:401: ValueError
____________ TestEvalplot.test_allow_probs_multiclass_emits_warning ____________

self = <scikitplot.seaborn.tests.test__confusion_matrix.TestEvalplot testMethod=test_allow_probs_multiclass_emits_warning>

    def test_allow_probs_multiclass_emits_warning(self):
        """
        Bug #4: evalplot wraps _prepare_subset ValueError as a UserWarning.

        The underlying validation error is confirmed in TestPrepareSubset;
        here we confirm the public API does not silently succeed.
        """
        rng = np.random.default_rng(2)
        n = 60
        df = pd.DataFrame({
            "x": rng.integers(0, 3, n),
            "y": np.clip(rng.uniform(0, 1, n), 0, 1),
        })
        with warnings.catch_warnings(record=True) as caught:
            warnings.simplefilter("always")
>           evalplot(df, x="x", y="y", kind="confusion_matrix", allow_probs=True)

scikitplot/seaborn/tests/test__confusion_matrix.py:288:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
scikitplot/seaborn/_confusion_matrix.py:1195: in evalplot
    p.plot_evalplot(
scikitplot/seaborn/_confusion_matrix.py:1032: in plot_evalplot
    artist_kws = self._artist_kws(
scikitplot/seaborn/_confusion_matrix.py:260: in _artist_kws
    kws["color"] = to_rgba(color, alpha)
                   ^^^^^^^^^^^^^^^^^^^^^
/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:317: in to_rgba
    rgba = _to_rgba_no_colorcycle(c, alpha)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

c = None, alpha = 1.0

    def _to_rgba_no_colorcycle(c, alpha=None):
        """
        Convert *c* to an RGBA color, with no support for color-cycle syntax.

        If *alpha* is given, force the alpha value of the returned RGBA tuple
        to *alpha*. Otherwise, the alpha value from *c* is used, if it has alpha
        information, or defaults to 1.

        *alpha* is ignored for the color value ``"none"`` (case-insensitive),
        which always maps to ``(0, 0, 0, 0)``.
        """
        if alpha is not None and not 0 <= alpha <= 1:
            raise ValueError("'alpha' must be between 0 and 1, inclusive")
        orig_c = c
        if c is np.ma.masked:
            return (0., 0., 0., 0.)
        if isinstance(c, str):
            if c.lower() == "none":
                return (0., 0., 0., 0.)
            # Named color.
            try:
                # This may turn c into a non-string, so we check again below.
                c = _colors_full_map[c]
            except KeyError:
                if len(orig_c) != 1:
                    try:
                        c = _colors_full_map[c.lower()]
                    except KeyError:
                        pass
        if isinstance(c, str):
            # hex color in #rrggbb format.
            match = re.match(r"\A#[a-fA-F0-9]{6}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1:3], c[3:5], c[5:7]])
                        + (alpha if alpha is not None else 1.,))
            # hex color in #rgb format, shorthand for #rrggbb.
            match = re.match(r"\A#[a-fA-F0-9]{3}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1]*2, c[2]*2, c[3]*2])
                        + (alpha if alpha is not None else 1.,))
            # hex color with alpha in #rrggbbaa format.
            match = re.match(r"\A#[a-fA-F0-9]{8}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1:3], c[3:5], c[5:7], c[7:9]]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # hex color with alpha in #rgba format, shorthand for #rrggbbaa.
            match = re.match(r"\A#[a-fA-F0-9]{4}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1]*2, c[2]*2, c[3]*2, c[4]*2]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # string gray.
            try:
                c = float(c)
            except ValueError:
                pass
            else:
                if not (0 <= c <= 1):
                    raise ValueError(
                        f"Invalid string grayscale value {orig_c!r}. "
                        f"Value must be within 0-1 range")
                return c, c, c, alpha if alpha is not None else 1.
            raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
        # turn 2-D array into 1-D array
        if isinstance(c, np.ndarray):
            if c.ndim == 2 and c.shape[0] == 1:
                c = c.reshape(-1)
        # tuple color.
        if not np.iterable(c):
>           raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
E           ValueError: Invalid RGBA argument: None

/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:401: ValueError
_____________________ TestEvalplot.test_ax_param_respected _____________________

self = <scikitplot.seaborn.tests.test__confusion_matrix.TestEvalplot testMethod=test_ax_param_respected>

    def test_ax_param_respected(self):
        fig, ax = plt.subplots()
>       result = evalplot(_make_df(), x="x", y="y", kind="confusion_matrix", ax=ax)
                 ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

scikitplot/seaborn/tests/test__confusion_matrix.py:256:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
scikitplot/seaborn/_confusion_matrix.py:1195: in evalplot
    p.plot_evalplot(
scikitplot/seaborn/_confusion_matrix.py:1032: in plot_evalplot
    artist_kws = self._artist_kws(
scikitplot/seaborn/_confusion_matrix.py:260: in _artist_kws
    kws["color"] = to_rgba(color, alpha)
                   ^^^^^^^^^^^^^^^^^^^^^
/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:317: in to_rgba
    rgba = _to_rgba_no_colorcycle(c, alpha)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

c = None, alpha = 1.0

    def _to_rgba_no_colorcycle(c, alpha=None):
        """
        Convert *c* to an RGBA color, with no support for color-cycle syntax.

        If *alpha* is given, force the alpha value of the returned RGBA tuple
        to *alpha*. Otherwise, the alpha value from *c* is used, if it has alpha
        information, or defaults to 1.

        *alpha* is ignored for the color value ``"none"`` (case-insensitive),
        which always maps to ``(0, 0, 0, 0)``.
        """
        if alpha is not None and not 0 <= alpha <= 1:
            raise ValueError("'alpha' must be between 0 and 1, inclusive")
        orig_c = c
        if c is np.ma.masked:
            return (0., 0., 0., 0.)
        if isinstance(c, str):
            if c.lower() == "none":
                return (0., 0., 0., 0.)
            # Named color.
            try:
                # This may turn c into a non-string, so we check again below.
                c = _colors_full_map[c]
            except KeyError:
                if len(orig_c) != 1:
                    try:
                        c = _colors_full_map[c.lower()]
                    except KeyError:
                        pass
        if isinstance(c, str):
            # hex color in #rrggbb format.
            match = re.match(r"\A#[a-fA-F0-9]{6}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1:3], c[3:5], c[5:7]])
                        + (alpha if alpha is not None else 1.,))
            # hex color in #rgb format, shorthand for #rrggbb.
            match = re.match(r"\A#[a-fA-F0-9]{3}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1]*2, c[2]*2, c[3]*2])
                        + (alpha if alpha is not None else 1.,))
            # hex color with alpha in #rrggbbaa format.
            match = re.match(r"\A#[a-fA-F0-9]{8}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1:3], c[3:5], c[5:7], c[7:9]]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # hex color with alpha in #rgba format, shorthand for #rrggbbaa.
            match = re.match(r"\A#[a-fA-F0-9]{4}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1]*2, c[2]*2, c[3]*2, c[4]*2]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # string gray.
            try:
                c = float(c)
            except ValueError:
                pass
            else:
                if not (0 <= c <= 1):
                    raise ValueError(
                        f"Invalid string grayscale value {orig_c!r}. "
                        f"Value must be within 0-1 range")
                return c, c, c, alpha if alpha is not None else 1.
            raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
        # turn 2-D array into 1-D array
        if isinstance(c, np.ndarray):
            if c.ndim == 2 and c.shape[0] == 1:
                c = c.reshape(-1)
        # tuple color.
        if not np.iterable(c):
>           raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
E           ValueError: Invalid RGBA argument: None

/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:401: ValueError
___________________ TestEvalplot.test_kind_case_insensitive ____________________

self = <scikitplot.seaborn.tests.test__confusion_matrix.TestEvalplot testMethod=test_kind_case_insensitive>

    def test_kind_case_insensitive(self):
>       ax = evalplot(_make_df(), x="x", y="y", kind="Confusion_Matrix")
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

scikitplot/seaborn/tests/test__confusion_matrix.py:297:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
scikitplot/seaborn/_confusion_matrix.py:1195: in evalplot
    p.plot_evalplot(
scikitplot/seaborn/_confusion_matrix.py:1032: in plot_evalplot
    artist_kws = self._artist_kws(
scikitplot/seaborn/_confusion_matrix.py:260: in _artist_kws
    kws["color"] = to_rgba(color, alpha)
                   ^^^^^^^^^^^^^^^^^^^^^
/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:317: in to_rgba
    rgba = _to_rgba_no_colorcycle(c, alpha)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

c = None, alpha = 1.0

    def _to_rgba_no_colorcycle(c, alpha=None):
        """
        Convert *c* to an RGBA color, with no support for color-cycle syntax.

        If *alpha* is given, force the alpha value of the returned RGBA tuple
        to *alpha*. Otherwise, the alpha value from *c* is used, if it has alpha
        information, or defaults to 1.

        *alpha* is ignored for the color value ``"none"`` (case-insensitive),
        which always maps to ``(0, 0, 0, 0)``.
        """
        if alpha is not None and not 0 <= alpha <= 1:
            raise ValueError("'alpha' must be between 0 and 1, inclusive")
        orig_c = c
        if c is np.ma.masked:
            return (0., 0., 0., 0.)
        if isinstance(c, str):
            if c.lower() == "none":
                return (0., 0., 0., 0.)
            # Named color.
            try:
                # This may turn c into a non-string, so we check again below.
                c = _colors_full_map[c]
            except KeyError:
                if len(orig_c) != 1:
                    try:
                        c = _colors_full_map[c.lower()]
                    except KeyError:
                        pass
        if isinstance(c, str):
            # hex color in #rrggbb format.
            match = re.match(r"\A#[a-fA-F0-9]{6}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1:3], c[3:5], c[5:7]])
                        + (alpha if alpha is not None else 1.,))
            # hex color in #rgb format, shorthand for #rrggbb.
            match = re.match(r"\A#[a-fA-F0-9]{3}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1]*2, c[2]*2, c[3]*2])
                        + (alpha if alpha is not None else 1.,))
            # hex color with alpha in #rrggbbaa format.
            match = re.match(r"\A#[a-fA-F0-9]{8}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1:3], c[3:5], c[5:7], c[7:9]]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # hex color with alpha in #rgba format, shorthand for #rrggbbaa.
            match = re.match(r"\A#[a-fA-F0-9]{4}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1]*2, c[2]*2, c[3]*2, c[4]*2]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # string gray.
            try:
                c = float(c)
            except ValueError:
                pass
            else:
                if not (0 <= c <= 1):
                    raise ValueError(
                        f"Invalid string grayscale value {orig_c!r}. "
                        f"Value must be within 0-1 range")
                return c, c, c, alpha if alpha is not None else 1.
            raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
        # turn 2-D array into 1-D array
        if isinstance(c, np.ndarray):
            if c.ndim == 2 and c.shape[0] == 1:
                c = c.reshape(-1)
        # tuple color.
        if not np.iterable(c):
>           raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
E           ValueError: Invalid RGBA argument: None

/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:401: ValueError
_________________ TestEvalplot.test_kind_none_defaults_to_all __________________

self = <scikitplot.seaborn.tests.test__confusion_matrix.TestEvalplot testMethod=test_kind_none_defaults_to_all>

    def test_kind_none_defaults_to_all(self):
>       ax = evalplot(_make_df(), x="x", y="y")
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

scikitplot/seaborn/tests/test__confusion_matrix.py:242:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
scikitplot/seaborn/_confusion_matrix.py:1195: in evalplot
    p.plot_evalplot(
scikitplot/seaborn/_confusion_matrix.py:1032: in plot_evalplot
    artist_kws = self._artist_kws(
scikitplot/seaborn/_confusion_matrix.py:260: in _artist_kws
    kws["color"] = to_rgba(color, alpha)
                   ^^^^^^^^^^^^^^^^^^^^^
/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:317: in to_rgba
    rgba = _to_rgba_no_colorcycle(c, alpha)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

c = None, alpha = 1.0

    def _to_rgba_no_colorcycle(c, alpha=None):
        """
        Convert *c* to an RGBA color, with no support for color-cycle syntax.

        If *alpha* is given, force the alpha value of the returned RGBA tuple
        to *alpha*. Otherwise, the alpha value from *c* is used, if it has alpha
        information, or defaults to 1.

        *alpha* is ignored for the color value ``"none"`` (case-insensitive),
        which always maps to ``(0, 0, 0, 0)``.
        """
        if alpha is not None and not 0 <= alpha <= 1:
            raise ValueError("'alpha' must be between 0 and 1, inclusive")
        orig_c = c
        if c is np.ma.masked:
            return (0., 0., 0., 0.)
        if isinstance(c, str):
            if c.lower() == "none":
                return (0., 0., 0., 0.)
            # Named color.
            try:
                # This may turn c into a non-string, so we check again below.
                c = _colors_full_map[c]
            except KeyError:
                if len(orig_c) != 1:
                    try:
                        c = _colors_full_map[c.lower()]
                    except KeyError:
                        pass
        if isinstance(c, str):
            # hex color in #rrggbb format.
            match = re.match(r"\A#[a-fA-F0-9]{6}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1:3], c[3:5], c[5:7]])
                        + (alpha if alpha is not None else 1.,))
            # hex color in #rgb format, shorthand for #rrggbb.
            match = re.match(r"\A#[a-fA-F0-9]{3}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1]*2, c[2]*2, c[3]*2])
                        + (alpha if alpha is not None else 1.,))
            # hex color with alpha in #rrggbbaa format.
            match = re.match(r"\A#[a-fA-F0-9]{8}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1:3], c[3:5], c[5:7], c[7:9]]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # hex color with alpha in #rgba format, shorthand for #rrggbbaa.
            match = re.match(r"\A#[a-fA-F0-9]{4}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1]*2, c[2]*2, c[3]*2, c[4]*2]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # string gray.
            try:
                c = float(c)
            except ValueError:
                pass
            else:
                if not (0 <= c <= 1):
                    raise ValueError(
                        f"Invalid string grayscale value {orig_c!r}. "
                        f"Value must be within 0-1 range")
                return c, c, c, alpha if alpha is not None else 1.
            raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
        # turn 2-D array into 1-D array
        if isinstance(c, np.ndarray):
            if c.ndim == 2 and c.shape[0] == 1:
                c = c.reshape(-1)
        # tuple color.
        if not np.iterable(c):
>           raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
E           ValueError: Invalid RGBA argument: None

/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:401: ValueError
______________________ TestEvalplot.test_minimum_dataset _______________________

self = <scikitplot.seaborn.tests.test__confusion_matrix.TestEvalplot testMethod=test_minimum_dataset>

    def test_minimum_dataset(self):
        df = pd.DataFrame({"x": [0, 1], "y": [0, 1]})
>       ax = evalplot(df, x="x", y="y", kind="confusion_matrix")
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

scikitplot/seaborn/tests/test__confusion_matrix.py:302:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
scikitplot/seaborn/_confusion_matrix.py:1195: in evalplot
    p.plot_evalplot(
scikitplot/seaborn/_confusion_matrix.py:1032: in plot_evalplot
    artist_kws = self._artist_kws(
scikitplot/seaborn/_confusion_matrix.py:260: in _artist_kws
    kws["color"] = to_rgba(color, alpha)
                   ^^^^^^^^^^^^^^^^^^^^^
/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:317: in to_rgba
    rgba = _to_rgba_no_colorcycle(c, alpha)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

c = None, alpha = 1.0

    def _to_rgba_no_colorcycle(c, alpha=None):
        """
        Convert *c* to an RGBA color, with no support for color-cycle syntax.

        If *alpha* is given, force the alpha value of the returned RGBA tuple
        to *alpha*. Otherwise, the alpha value from *c* is used, if it has alpha
        information, or defaults to 1.

        *alpha* is ignored for the color value ``"none"`` (case-insensitive),
        which always maps to ``(0, 0, 0, 0)``.
        """
        if alpha is not None and not 0 <= alpha <= 1:
            raise ValueError("'alpha' must be between 0 and 1, inclusive")
        orig_c = c
        if c is np.ma.masked:
            return (0., 0., 0., 0.)
        if isinstance(c, str):
            if c.lower() == "none":
                return (0., 0., 0., 0.)
            # Named color.
            try:
                # This may turn c into a non-string, so we check again below.
                c = _colors_full_map[c]
            except KeyError:
                if len(orig_c) != 1:
                    try:
                        c = _colors_full_map[c.lower()]
                    except KeyError:
                        pass
        if isinstance(c, str):
            # hex color in #rrggbb format.
            match = re.match(r"\A#[a-fA-F0-9]{6}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1:3], c[3:5], c[5:7]])
                        + (alpha if alpha is not None else 1.,))
            # hex color in #rgb format, shorthand for #rrggbb.
            match = re.match(r"\A#[a-fA-F0-9]{3}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1]*2, c[2]*2, c[3]*2])
                        + (alpha if alpha is not None else 1.,))
            # hex color with alpha in #rrggbbaa format.
            match = re.match(r"\A#[a-fA-F0-9]{8}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1:3], c[3:5], c[5:7], c[7:9]]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # hex color with alpha in #rgba format, shorthand for #rrggbbaa.
            match = re.match(r"\A#[a-fA-F0-9]{4}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1]*2, c[2]*2, c[3]*2, c[4]*2]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # string gray.
            try:
                c = float(c)
            except ValueError:
                pass
            else:
                if not (0 <= c <= 1):
                    raise ValueError(
                        f"Invalid string grayscale value {orig_c!r}. "
                        f"Value must be within 0-1 range")
                return c, c, c, alpha if alpha is not None else 1.
            raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
        # turn 2-D array into 1-D array
        if isinstance(c, np.ndarray):
            if c.ndim == 2 and c.shape[0] == 1:
                c = c.reshape(-1)
        # tuple color.
        if not np.iterable(c):
>           raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
E           ValueError: Invalid RGBA argument: None

/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:401: ValueError
____________________ TestEvalplot.test_multiclass_no_crash _____________________

self = <scikitplot.seaborn.tests.test__confusion_matrix.TestEvalplot testMethod=test_multiclass_no_crash>

    def test_multiclass_no_crash(self):
>       ax = evalplot(_make_df(multiclass=True), x="x", y="y", kind="confusion_matrix")
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

scikitplot/seaborn/tests/test__confusion_matrix.py:260:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
scikitplot/seaborn/_confusion_matrix.py:1195: in evalplot
    p.plot_evalplot(
scikitplot/seaborn/_confusion_matrix.py:1032: in plot_evalplot
    artist_kws = self._artist_kws(
scikitplot/seaborn/_confusion_matrix.py:260: in _artist_kws
    kws["color"] = to_rgba(color, alpha)
                   ^^^^^^^^^^^^^^^^^^^^^
/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:317: in to_rgba
    rgba = _to_rgba_no_colorcycle(c, alpha)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

c = None, alpha = 1.0

    def _to_rgba_no_colorcycle(c, alpha=None):
        """
        Convert *c* to an RGBA color, with no support for color-cycle syntax.

        If *alpha* is given, force the alpha value of the returned RGBA tuple
        to *alpha*. Otherwise, the alpha value from *c* is used, if it has alpha
        information, or defaults to 1.

        *alpha* is ignored for the color value ``"none"`` (case-insensitive),
        which always maps to ``(0, 0, 0, 0)``.
        """
        if alpha is not None and not 0 <= alpha <= 1:
            raise ValueError("'alpha' must be between 0 and 1, inclusive")
        orig_c = c
        if c is np.ma.masked:
            return (0., 0., 0., 0.)
        if isinstance(c, str):
            if c.lower() == "none":
                return (0., 0., 0., 0.)
            # Named color.
            try:
                # This may turn c into a non-string, so we check again below.
                c = _colors_full_map[c]
            except KeyError:
                if len(orig_c) != 1:
                    try:
                        c = _colors_full_map[c.lower()]
                    except KeyError:
                        pass
        if isinstance(c, str):
            # hex color in #rrggbb format.
            match = re.match(r"\A#[a-fA-F0-9]{6}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1:3], c[3:5], c[5:7]])
                        + (alpha if alpha is not None else 1.,))
            # hex color in #rgb format, shorthand for #rrggbb.
            match = re.match(r"\A#[a-fA-F0-9]{3}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1]*2, c[2]*2, c[3]*2])
                        + (alpha if alpha is not None else 1.,))
            # hex color with alpha in #rrggbbaa format.
            match = re.match(r"\A#[a-fA-F0-9]{8}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1:3], c[3:5], c[5:7], c[7:9]]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # hex color with alpha in #rgba format, shorthand for #rrggbbaa.
            match = re.match(r"\A#[a-fA-F0-9]{4}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1]*2, c[2]*2, c[3]*2, c[4]*2]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # string gray.
            try:
                c = float(c)
            except ValueError:
                pass
            else:
                if not (0 <= c <= 1):
                    raise ValueError(
                        f"Invalid string grayscale value {orig_c!r}. "
                        f"Value must be within 0-1 range")
                return c, c, c, alpha if alpha is not None else 1.
            raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
        # turn 2-D array into 1-D array
        if isinstance(c, np.ndarray):
            if c.ndim == 2 and c.shape[0] == 1:
                c = c.reshape(-1)
        # tuple color.
        if not np.iterable(c):
>           raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
E           ValueError: Invalid RGBA argument: None

/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:401: ValueError
______________________ TestEvalplot.test_returns_axes_all ______________________

self = <scikitplot.seaborn.tests.test__confusion_matrix.TestEvalplot testMethod=test_returns_axes_all>

    def test_returns_axes_all(self):
>       ax = evalplot(_make_df(), x="x", y="y", kind="all")
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

scikitplot/seaborn/tests/test__confusion_matrix.py:238:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
scikitplot/seaborn/_confusion_matrix.py:1195: in evalplot
    p.plot_evalplot(
scikitplot/seaborn/_confusion_matrix.py:1032: in plot_evalplot
    artist_kws = self._artist_kws(
scikitplot/seaborn/_confusion_matrix.py:260: in _artist_kws
    kws["color"] = to_rgba(color, alpha)
                   ^^^^^^^^^^^^^^^^^^^^^
/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:317: in to_rgba
    rgba = _to_rgba_no_colorcycle(c, alpha)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

c = None, alpha = 1.0

    def _to_rgba_no_colorcycle(c, alpha=None):
        """
        Convert *c* to an RGBA color, with no support for color-cycle syntax.

        If *alpha* is given, force the alpha value of the returned RGBA tuple
        to *alpha*. Otherwise, the alpha value from *c* is used, if it has alpha
        information, or defaults to 1.

        *alpha* is ignored for the color value ``"none"`` (case-insensitive),
        which always maps to ``(0, 0, 0, 0)``.
        """
        if alpha is not None and not 0 <= alpha <= 1:
            raise ValueError("'alpha' must be between 0 and 1, inclusive")
        orig_c = c
        if c is np.ma.masked:
            return (0., 0., 0., 0.)
        if isinstance(c, str):
            if c.lower() == "none":
                return (0., 0., 0., 0.)
            # Named color.
            try:
                # This may turn c into a non-string, so we check again below.
                c = _colors_full_map[c]
            except KeyError:
                if len(orig_c) != 1:
                    try:
                        c = _colors_full_map[c.lower()]
                    except KeyError:
                        pass
        if isinstance(c, str):
            # hex color in #rrggbb format.
            match = re.match(r"\A#[a-fA-F0-9]{6}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1:3], c[3:5], c[5:7]])
                        + (alpha if alpha is not None else 1.,))
            # hex color in #rgb format, shorthand for #rrggbb.
            match = re.match(r"\A#[a-fA-F0-9]{3}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1]*2, c[2]*2, c[3]*2])
                        + (alpha if alpha is not None else 1.,))
            # hex color with alpha in #rrggbbaa format.
            match = re.match(r"\A#[a-fA-F0-9]{8}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1:3], c[3:5], c[5:7], c[7:9]]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # hex color with alpha in #rgba format, shorthand for #rrggbbaa.
            match = re.match(r"\A#[a-fA-F0-9]{4}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1]*2, c[2]*2, c[3]*2, c[4]*2]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # string gray.
            try:
                c = float(c)
            except ValueError:
                pass
            else:
                if not (0 <= c <= 1):
                    raise ValueError(
                        f"Invalid string grayscale value {orig_c!r}. "
                        f"Value must be within 0-1 range")
                return c, c, c, alpha if alpha is not None else 1.
            raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
        # turn 2-D array into 1-D array
        if isinstance(c, np.ndarray):
            if c.ndim == 2 and c.shape[0] == 1:
                c = c.reshape(-1)
        # tuple color.
        if not np.iterable(c):
>           raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
E           ValueError: Invalid RGBA argument: None

/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:401: ValueError
_____________ TestEvalplot.test_returns_axes_classification_report _____________

self = <scikitplot.seaborn.tests.test__confusion_matrix.TestEvalplot testMethod=test_returns_axes_classification_report>

    def test_returns_axes_classification_report(self):
>       ax = evalplot(_make_df(), x="x", y="y", kind="classification_report")
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

scikitplot/seaborn/tests/test__confusion_matrix.py:234:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
scikitplot/seaborn/_confusion_matrix.py:1195: in evalplot
    p.plot_evalplot(
scikitplot/seaborn/_confusion_matrix.py:1032: in plot_evalplot
    artist_kws = self._artist_kws(
scikitplot/seaborn/_confusion_matrix.py:260: in _artist_kws
    kws["color"] = to_rgba(color, alpha)
                   ^^^^^^^^^^^^^^^^^^^^^
/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:317: in to_rgba
    rgba = _to_rgba_no_colorcycle(c, alpha)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

c = None, alpha = 1.0

    def _to_rgba_no_colorcycle(c, alpha=None):
        """
        Convert *c* to an RGBA color, with no support for color-cycle syntax.

        If *alpha* is given, force the alpha value of the returned RGBA tuple
        to *alpha*. Otherwise, the alpha value from *c* is used, if it has alpha
        information, or defaults to 1.

        *alpha* is ignored for the color value ``"none"`` (case-insensitive),
        which always maps to ``(0, 0, 0, 0)``.
        """
        if alpha is not None and not 0 <= alpha <= 1:
            raise ValueError("'alpha' must be between 0 and 1, inclusive")
        orig_c = c
        if c is np.ma.masked:
            return (0., 0., 0., 0.)
        if isinstance(c, str):
            if c.lower() == "none":
                return (0., 0., 0., 0.)
            # Named color.
            try:
                # This may turn c into a non-string, so we check again below.
                c = _colors_full_map[c]
            except KeyError:
                if len(orig_c) != 1:
                    try:
                        c = _colors_full_map[c.lower()]
                    except KeyError:
                        pass
        if isinstance(c, str):
            # hex color in #rrggbb format.
            match = re.match(r"\A#[a-fA-F0-9]{6}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1:3], c[3:5], c[5:7]])
                        + (alpha if alpha is not None else 1.,))
            # hex color in #rgb format, shorthand for #rrggbb.
            match = re.match(r"\A#[a-fA-F0-9]{3}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1]*2, c[2]*2, c[3]*2])
                        + (alpha if alpha is not None else 1.,))
            # hex color with alpha in #rrggbbaa format.
            match = re.match(r"\A#[a-fA-F0-9]{8}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1:3], c[3:5], c[5:7], c[7:9]]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # hex color with alpha in #rgba format, shorthand for #rrggbbaa.
            match = re.match(r"\A#[a-fA-F0-9]{4}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1]*2, c[2]*2, c[3]*2, c[4]*2]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # string gray.
            try:
                c = float(c)
            except ValueError:
                pass
            else:
                if not (0 <= c <= 1):
                    raise ValueError(
                        f"Invalid string grayscale value {orig_c!r}. "
                        f"Value must be within 0-1 range")
                return c, c, c, alpha if alpha is not None else 1.
            raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
        # turn 2-D array into 1-D array
        if isinstance(c, np.ndarray):
            if c.ndim == 2 and c.shape[0] == 1:
                c = c.reshape(-1)
        # tuple color.
        if not np.iterable(c):
>           raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
E           ValueError: Invalid RGBA argument: None

/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:401: ValueError
_______________ TestEvalplot.test_returns_axes_confusion_matrix ________________

self = <scikitplot.seaborn.tests.test__confusion_matrix.TestEvalplot testMethod=test_returns_axes_confusion_matrix>

    def test_returns_axes_confusion_matrix(self):
>       ax = evalplot(_make_df(), x="x", y="y", kind="confusion_matrix")
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

scikitplot/seaborn/tests/test__confusion_matrix.py:230:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
scikitplot/seaborn/_confusion_matrix.py:1195: in evalplot
    p.plot_evalplot(
scikitplot/seaborn/_confusion_matrix.py:1032: in plot_evalplot
    artist_kws = self._artist_kws(
scikitplot/seaborn/_confusion_matrix.py:260: in _artist_kws
    kws["color"] = to_rgba(color, alpha)
                   ^^^^^^^^^^^^^^^^^^^^^
/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:317: in to_rgba
    rgba = _to_rgba_no_colorcycle(c, alpha)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

c = None, alpha = 1.0

    def _to_rgba_no_colorcycle(c, alpha=None):
        """
        Convert *c* to an RGBA color, with no support for color-cycle syntax.

        If *alpha* is given, force the alpha value of the returned RGBA tuple
        to *alpha*. Otherwise, the alpha value from *c* is used, if it has alpha
        information, or defaults to 1.

        *alpha* is ignored for the color value ``"none"`` (case-insensitive),
        which always maps to ``(0, 0, 0, 0)``.
        """
        if alpha is not None and not 0 <= alpha <= 1:
            raise ValueError("'alpha' must be between 0 and 1, inclusive")
        orig_c = c
        if c is np.ma.masked:
            return (0., 0., 0., 0.)
        if isinstance(c, str):
            if c.lower() == "none":
                return (0., 0., 0., 0.)
            # Named color.
            try:
                # This may turn c into a non-string, so we check again below.
                c = _colors_full_map[c]
            except KeyError:
                if len(orig_c) != 1:
                    try:
                        c = _colors_full_map[c.lower()]
                    except KeyError:
                        pass
        if isinstance(c, str):
            # hex color in #rrggbb format.
            match = re.match(r"\A#[a-fA-F0-9]{6}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1:3], c[3:5], c[5:7]])
                        + (alpha if alpha is not None else 1.,))
            # hex color in #rgb format, shorthand for #rrggbb.
            match = re.match(r"\A#[a-fA-F0-9]{3}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1]*2, c[2]*2, c[3]*2])
                        + (alpha if alpha is not None else 1.,))
            # hex color with alpha in #rrggbbaa format.
            match = re.match(r"\A#[a-fA-F0-9]{8}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1:3], c[3:5], c[5:7], c[7:9]]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # hex color with alpha in #rgba format, shorthand for #rrggbbaa.
            match = re.match(r"\A#[a-fA-F0-9]{4}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1]*2, c[2]*2, c[3]*2, c[4]*2]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # string gray.
            try:
                c = float(c)
            except ValueError:
                pass
            else:
                if not (0 <= c <= 1):
                    raise ValueError(
                        f"Invalid string grayscale value {orig_c!r}. "
                        f"Value must be within 0-1 range")
                return c, c, c, alpha if alpha is not None else 1.
            raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
        # turn 2-D array into 1-D array
        if isinstance(c, np.ndarray):
            if c.ndim == 2 and c.shape[0] == 1:
                c = c.reshape(-1)
        # tuple color.
        if not np.iterable(c):
>           raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
E           ValueError: Invalid RGBA argument: None

/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:401: ValueError
___________________ TestDecileplot.test_accepts_numpy_arrays ___________________

self = <scikitplot.seaborn.tests.test__decile.TestDecileplot testMethod=test_accepts_numpy_arrays>

    def test_accepts_numpy_arrays(self):
        y_true, y_score = _make_data()
>       ax = decileplot(x=y_true, y=y_score, kind="cumulative_lift")
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

scikitplot/seaborn/tests/test__decile.py:365:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
scikitplot/seaborn/_decile.py:1823: in decileplot
    dt = p.plot_decileplot(
scikitplot/seaborn/_decile.py:1645: in plot_decileplot
    artist_kws = self._artist_kws(
scikitplot/seaborn/_decile.py:504: in _artist_kws
    kws["color"] = to_rgba(color, alpha)
                   ^^^^^^^^^^^^^^^^^^^^^
/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:317: in to_rgba
    rgba = _to_rgba_no_colorcycle(c, alpha)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

c = None, alpha = 1.0

    def _to_rgba_no_colorcycle(c, alpha=None):
        """
        Convert *c* to an RGBA color, with no support for color-cycle syntax.

        If *alpha* is given, force the alpha value of the returned RGBA tuple
        to *alpha*. Otherwise, the alpha value from *c* is used, if it has alpha
        information, or defaults to 1.

        *alpha* is ignored for the color value ``"none"`` (case-insensitive),
        which always maps to ``(0, 0, 0, 0)``.
        """
        if alpha is not None and not 0 <= alpha <= 1:
            raise ValueError("'alpha' must be between 0 and 1, inclusive")
        orig_c = c
        if c is np.ma.masked:
            return (0., 0., 0., 0.)
        if isinstance(c, str):
            if c.lower() == "none":
                return (0., 0., 0., 0.)
            # Named color.
            try:
                # This may turn c into a non-string, so we check again below.
                c = _colors_full_map[c]
            except KeyError:
                if len(orig_c) != 1:
                    try:
                        c = _colors_full_map[c.lower()]
                    except KeyError:
                        pass
        if isinstance(c, str):
            # hex color in #rrggbb format.
            match = re.match(r"\A#[a-fA-F0-9]{6}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1:3], c[3:5], c[5:7]])
                        + (alpha if alpha is not None else 1.,))
            # hex color in #rgb format, shorthand for #rrggbb.
            match = re.match(r"\A#[a-fA-F0-9]{3}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1]*2, c[2]*2, c[3]*2])
                        + (alpha if alpha is not None else 1.,))
            # hex color with alpha in #rrggbbaa format.
            match = re.match(r"\A#[a-fA-F0-9]{8}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1:3], c[3:5], c[5:7], c[7:9]]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # hex color with alpha in #rgba format, shorthand for #rrggbbaa.
            match = re.match(r"\A#[a-fA-F0-9]{4}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1]*2, c[2]*2, c[3]*2, c[4]*2]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # string gray.
            try:
                c = float(c)
            except ValueError:
                pass
            else:
                if not (0 <= c <= 1):
                    raise ValueError(
                        f"Invalid string grayscale value {orig_c!r}. "
                        f"Value must be within 0-1 range")
                return c, c, c, alpha if alpha is not None else 1.
            raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
        # turn 2-D array into 1-D array
        if isinstance(c, np.ndarray):
            if c.ndim == 2 and c.shape[0] == 1:
                c = c.reshape(-1)
        # tuple color.
        if not np.iterable(c):
>           raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
E           ValueError: Invalid RGBA argument: None

/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:401: ValueError
____________________ TestDecileplot.test_ax_param_respected ____________________

self = <scikitplot.seaborn.tests.test__decile.TestDecileplot testMethod=test_ax_param_respected>

    def test_ax_param_respected(self):
        fig, ax = plt.subplots()
>       result = decileplot(_make_df(), x="x", y="y", kind="cumulative_lift", ax=ax)
                 ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

scikitplot/seaborn/tests/test__decile.py:360:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
scikitplot/seaborn/_decile.py:1823: in decileplot
    dt = p.plot_decileplot(
scikitplot/seaborn/_decile.py:1645: in plot_decileplot
    artist_kws = self._artist_kws(
scikitplot/seaborn/_decile.py:504: in _artist_kws
    kws["color"] = to_rgba(color, alpha)
                   ^^^^^^^^^^^^^^^^^^^^^
/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:317: in to_rgba
    rgba = _to_rgba_no_colorcycle(c, alpha)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

c = None, alpha = 1.0

    def _to_rgba_no_colorcycle(c, alpha=None):
        """
        Convert *c* to an RGBA color, with no support for color-cycle syntax.

        If *alpha* is given, force the alpha value of the returned RGBA tuple
        to *alpha*. Otherwise, the alpha value from *c* is used, if it has alpha
        information, or defaults to 1.

        *alpha* is ignored for the color value ``"none"`` (case-insensitive),
        which always maps to ``(0, 0, 0, 0)``.
        """
        if alpha is not None and not 0 <= alpha <= 1:
            raise ValueError("'alpha' must be between 0 and 1, inclusive")
        orig_c = c
        if c is np.ma.masked:
            return (0., 0., 0., 0.)
        if isinstance(c, str):
            if c.lower() == "none":
                return (0., 0., 0., 0.)
            # Named color.
            try:
                # This may turn c into a non-string, so we check again below.
                c = _colors_full_map[c]
            except KeyError:
                if len(orig_c) != 1:
                    try:
                        c = _colors_full_map[c.lower()]
                    except KeyError:
                        pass
        if isinstance(c, str):
            # hex color in #rrggbb format.
            match = re.match(r"\A#[a-fA-F0-9]{6}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1:3], c[3:5], c[5:7]])
                        + (alpha if alpha is not None else 1.,))
            # hex color in #rgb format, shorthand for #rrggbb.
            match = re.match(r"\A#[a-fA-F0-9]{3}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1]*2, c[2]*2, c[3]*2])
                        + (alpha if alpha is not None else 1.,))
            # hex color with alpha in #rrggbbaa format.
            match = re.match(r"\A#[a-fA-F0-9]{8}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1:3], c[3:5], c[5:7], c[7:9]]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # hex color with alpha in #rgba format, shorthand for #rrggbbaa.
            match = re.match(r"\A#[a-fA-F0-9]{4}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1]*2, c[2]*2, c[3]*2, c[4]*2]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # string gray.
            try:
                c = float(c)
            except ValueError:
                pass
            else:
                if not (0 <= c <= 1):
                    raise ValueError(
                        f"Invalid string grayscale value {orig_c!r}. "
                        f"Value must be within 0-1 range")
                return c, c, c, alpha if alpha is not None else 1.
            raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
        # turn 2-D array into 1-D array
        if isinstance(c, np.ndarray):
            if c.ndim == 2 and c.shape[0] == 1:
                c = c.reshape(-1)
        # tuple color.
        if not np.iterable(c):
>           raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
E           ValueError: Invalid RGBA argument: None

/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:401: ValueError
__________________ TestDecileplot.test_kind_case_insensitive ___________________

self = <scikitplot.seaborn.tests.test__decile.TestDecileplot testMethod=test_kind_case_insensitive>

    def test_kind_case_insensitive(self):
>       ax = decileplot(_make_df(), x="x", y="y", kind="Cumulative_Lift")
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

scikitplot/seaborn/tests/test__decile.py:373:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
scikitplot/seaborn/_decile.py:1823: in decileplot
    dt = p.plot_decileplot(
scikitplot/seaborn/_decile.py:1645: in plot_decileplot
    artist_kws = self._artist_kws(
scikitplot/seaborn/_decile.py:504: in _artist_kws
    kws["color"] = to_rgba(color, alpha)
                   ^^^^^^^^^^^^^^^^^^^^^
/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:317: in to_rgba
    rgba = _to_rgba_no_colorcycle(c, alpha)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

c = None, alpha = 1.0

    def _to_rgba_no_colorcycle(c, alpha=None):
        """
        Convert *c* to an RGBA color, with no support for color-cycle syntax.

        If *alpha* is given, force the alpha value of the returned RGBA tuple
        to *alpha*. Otherwise, the alpha value from *c* is used, if it has alpha
        information, or defaults to 1.

        *alpha* is ignored for the color value ``"none"`` (case-insensitive),
        which always maps to ``(0, 0, 0, 0)``.
        """
        if alpha is not None and not 0 <= alpha <= 1:
            raise ValueError("'alpha' must be between 0 and 1, inclusive")
        orig_c = c
        if c is np.ma.masked:
            return (0., 0., 0., 0.)
        if isinstance(c, str):
            if c.lower() == "none":
                return (0., 0., 0., 0.)
            # Named color.
            try:
                # This may turn c into a non-string, so we check again below.
                c = _colors_full_map[c]
            except KeyError:
                if len(orig_c) != 1:
                    try:
                        c = _colors_full_map[c.lower()]
                    except KeyError:
                        pass
        if isinstance(c, str):
            # hex color in #rrggbb format.
            match = re.match(r"\A#[a-fA-F0-9]{6}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1:3], c[3:5], c[5:7]])
                        + (alpha if alpha is not None else 1.,))
            # hex color in #rgb format, shorthand for #rrggbb.
            match = re.match(r"\A#[a-fA-F0-9]{3}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1]*2, c[2]*2, c[3]*2])
                        + (alpha if alpha is not None else 1.,))
            # hex color with alpha in #rrggbbaa format.
            match = re.match(r"\A#[a-fA-F0-9]{8}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1:3], c[3:5], c[5:7], c[7:9]]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # hex color with alpha in #rgba format, shorthand for #rrggbbaa.
            match = re.match(r"\A#[a-fA-F0-9]{4}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1]*2, c[2]*2, c[3]*2, c[4]*2]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # string gray.
            try:
                c = float(c)
            except ValueError:
                pass
            else:
                if not (0 <= c <= 1):
                    raise ValueError(
                        f"Invalid string grayscale value {orig_c!r}. "
                        f"Value must be within 0-1 range")
                return c, c, c, alpha if alpha is not None else 1.
            raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
        # turn 2-D array into 1-D array
        if isinstance(c, np.ndarray):
            if c.ndim == 2 and c.shape[0] == 1:
                c = c.reshape(-1)
        # tuple color.
        if not np.iterable(c):
>           raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
E           ValueError: Invalid RGBA argument: None

/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:401: ValueError
____________ TestDecileplot.test_kind_cumulative_gain_returns_axes _____________

self = <scikitplot.seaborn.tests.test__decile.TestDecileplot testMethod=test_kind_cumulative_gain_returns_axes>

    def test_kind_cumulative_gain_returns_axes(self):
>       ax = decileplot(_make_df(), x="x", y="y", kind="cumulative_gain")
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

scikitplot/seaborn/tests/test__decile.py:334:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
scikitplot/seaborn/_decile.py:1823: in decileplot
    dt = p.plot_decileplot(
scikitplot/seaborn/_decile.py:1645: in plot_decileplot
    artist_kws = self._artist_kws(
scikitplot/seaborn/_decile.py:504: in _artist_kws
    kws["color"] = to_rgba(color, alpha)
                   ^^^^^^^^^^^^^^^^^^^^^
/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:317: in to_rgba
    rgba = _to_rgba_no_colorcycle(c, alpha)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

c = None, alpha = 1.0

    def _to_rgba_no_colorcycle(c, alpha=None):
        """
        Convert *c* to an RGBA color, with no support for color-cycle syntax.

        If *alpha* is given, force the alpha value of the returned RGBA tuple
        to *alpha*. Otherwise, the alpha value from *c* is used, if it has alpha
        information, or defaults to 1.

        *alpha* is ignored for the color value ``"none"`` (case-insensitive),
        which always maps to ``(0, 0, 0, 0)``.
        """
        if alpha is not None and not 0 <= alpha <= 1:
            raise ValueError("'alpha' must be between 0 and 1, inclusive")
        orig_c = c
        if c is np.ma.masked:
            return (0., 0., 0., 0.)
        if isinstance(c, str):
            if c.lower() == "none":
                return (0., 0., 0., 0.)
            # Named color.
            try:
                # This may turn c into a non-string, so we check again below.
                c = _colors_full_map[c]
            except KeyError:
                if len(orig_c) != 1:
                    try:
                        c = _colors_full_map[c.lower()]
                    except KeyError:
                        pass
        if isinstance(c, str):
            # hex color in #rrggbb format.
            match = re.match(r"\A#[a-fA-F0-9]{6}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1:3], c[3:5], c[5:7]])
                        + (alpha if alpha is not None else 1.,))
            # hex color in #rgb format, shorthand for #rrggbb.
            match = re.match(r"\A#[a-fA-F0-9]{3}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1]*2, c[2]*2, c[3]*2])
                        + (alpha if alpha is not None else 1.,))
            # hex color with alpha in #rrggbbaa format.
            match = re.match(r"\A#[a-fA-F0-9]{8}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1:3], c[3:5], c[5:7], c[7:9]]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # hex color with alpha in #rgba format, shorthand for #rrggbbaa.
            match = re.match(r"\A#[a-fA-F0-9]{4}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1]*2, c[2]*2, c[3]*2, c[4]*2]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # string gray.
            try:
                c = float(c)
            except ValueError:
                pass
            else:
                if not (0 <= c <= 1):
                    raise ValueError(
                        f"Invalid string grayscale value {orig_c!r}. "
                        f"Value must be within 0-1 range")
                return c, c, c, alpha if alpha is not None else 1.
            raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
        # turn 2-D array into 1-D array
        if isinstance(c, np.ndarray):
            if c.ndim == 2 and c.shape[0] == 1:
                c = c.reshape(-1)
        # tuple color.
        if not np.iterable(c):
>           raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
E           ValueError: Invalid RGBA argument: None

/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:401: ValueError
____________ TestDecileplot.test_kind_cumulative_lift_returns_axes _____________

self = <scikitplot.seaborn.tests.test__decile.TestDecileplot testMethod=test_kind_cumulative_lift_returns_axes>

    def test_kind_cumulative_lift_returns_axes(self):
>       ax = decileplot(_make_df(), x="x", y="y", kind="cumulative_lift")
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

scikitplot/seaborn/tests/test__decile.py:326:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
scikitplot/seaborn/_decile.py:1823: in decileplot
    dt = p.plot_decileplot(
scikitplot/seaborn/_decile.py:1645: in plot_decileplot
    artist_kws = self._artist_kws(
scikitplot/seaborn/_decile.py:504: in _artist_kws
    kws["color"] = to_rgba(color, alpha)
                   ^^^^^^^^^^^^^^^^^^^^^
/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:317: in to_rgba
    rgba = _to_rgba_no_colorcycle(c, alpha)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

c = None, alpha = 1.0

    def _to_rgba_no_colorcycle(c, alpha=None):
        """
        Convert *c* to an RGBA color, with no support for color-cycle syntax.

        If *alpha* is given, force the alpha value of the returned RGBA tuple
        to *alpha*. Otherwise, the alpha value from *c* is used, if it has alpha
        information, or defaults to 1.

        *alpha* is ignored for the color value ``"none"`` (case-insensitive),
        which always maps to ``(0, 0, 0, 0)``.
        """
        if alpha is not None and not 0 <= alpha <= 1:
            raise ValueError("'alpha' must be between 0 and 1, inclusive")
        orig_c = c
        if c is np.ma.masked:
            return (0., 0., 0., 0.)
        if isinstance(c, str):
            if c.lower() == "none":
                return (0., 0., 0., 0.)
            # Named color.
            try:
                # This may turn c into a non-string, so we check again below.
                c = _colors_full_map[c]
            except KeyError:
                if len(orig_c) != 1:
                    try:
                        c = _colors_full_map[c.lower()]
                    except KeyError:
                        pass
        if isinstance(c, str):
            # hex color in #rrggbb format.
            match = re.match(r"\A#[a-fA-F0-9]{6}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1:3], c[3:5], c[5:7]])
                        + (alpha if alpha is not None else 1.,))
            # hex color in #rgb format, shorthand for #rrggbb.
            match = re.match(r"\A#[a-fA-F0-9]{3}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1]*2, c[2]*2, c[3]*2])
                        + (alpha if alpha is not None else 1.,))
            # hex color with alpha in #rrggbbaa format.
            match = re.match(r"\A#[a-fA-F0-9]{8}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1:3], c[3:5], c[5:7], c[7:9]]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # hex color with alpha in #rgba format, shorthand for #rrggbbaa.
            match = re.match(r"\A#[a-fA-F0-9]{4}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1]*2, c[2]*2, c[3]*2, c[4]*2]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # string gray.
            try:
                c = float(c)
            except ValueError:
                pass
            else:
                if not (0 <= c <= 1):
                    raise ValueError(
                        f"Invalid string grayscale value {orig_c!r}. "
                        f"Value must be within 0-1 range")
                return c, c, c, alpha if alpha is not None else 1.
            raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
        # turn 2-D array into 1-D array
        if isinstance(c, np.ndarray):
            if c.ndim == 2 and c.shape[0] == 1:
                c = c.reshape(-1)
        # tuple color.
        if not np.iterable(c):
>           raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
E           ValueError: Invalid RGBA argument: None

/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:401: ValueError
__________ TestDecileplot.test_kind_cumulative_response_returns_axes ___________

self = <scikitplot.seaborn.tests.test__decile.TestDecileplot testMethod=test_kind_cumulative_response_returns_axes>

    def test_kind_cumulative_response_returns_axes(self):
>       ax = decileplot(_make_df(), x="x", y="y", kind="cumulative_response")
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

scikitplot/seaborn/tests/test__decile.py:342:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
scikitplot/seaborn/_decile.py:1823: in decileplot
    dt = p.plot_decileplot(
scikitplot/seaborn/_decile.py:1645: in plot_decileplot
    artist_kws = self._artist_kws(
scikitplot/seaborn/_decile.py:504: in _artist_kws
    kws["color"] = to_rgba(color, alpha)
                   ^^^^^^^^^^^^^^^^^^^^^
/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:317: in to_rgba
    rgba = _to_rgba_no_colorcycle(c, alpha)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

c = None, alpha = 1.0

    def _to_rgba_no_colorcycle(c, alpha=None):
        """
        Convert *c* to an RGBA color, with no support for color-cycle syntax.

        If *alpha* is given, force the alpha value of the returned RGBA tuple
        to *alpha*. Otherwise, the alpha value from *c* is used, if it has alpha
        information, or defaults to 1.

        *alpha* is ignored for the color value ``"none"`` (case-insensitive),
        which always maps to ``(0, 0, 0, 0)``.
        """
        if alpha is not None and not 0 <= alpha <= 1:
            raise ValueError("'alpha' must be between 0 and 1, inclusive")
        orig_c = c
        if c is np.ma.masked:
            return (0., 0., 0., 0.)
        if isinstance(c, str):
            if c.lower() == "none":
                return (0., 0., 0., 0.)
            # Named color.
            try:
                # This may turn c into a non-string, so we check again below.
                c = _colors_full_map[c]
            except KeyError:
                if len(orig_c) != 1:
                    try:
                        c = _colors_full_map[c.lower()]
                    except KeyError:
                        pass
        if isinstance(c, str):
            # hex color in #rrggbb format.
            match = re.match(r"\A#[a-fA-F0-9]{6}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1:3], c[3:5], c[5:7]])
                        + (alpha if alpha is not None else 1.,))
            # hex color in #rgb format, shorthand for #rrggbb.
            match = re.match(r"\A#[a-fA-F0-9]{3}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1]*2, c[2]*2, c[3]*2])
                        + (alpha if alpha is not None else 1.,))
            # hex color with alpha in #rrggbbaa format.
            match = re.match(r"\A#[a-fA-F0-9]{8}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1:3], c[3:5], c[5:7], c[7:9]]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # hex color with alpha in #rgba format, shorthand for #rrggbbaa.
            match = re.match(r"\A#[a-fA-F0-9]{4}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1]*2, c[2]*2, c[3]*2, c[4]*2]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # string gray.
            try:
                c = float(c)
            except ValueError:
                pass
            else:
                if not (0 <= c <= 1):
                    raise ValueError(
                        f"Invalid string grayscale value {orig_c!r}. "
                        f"Value must be within 0-1 range")
                return c, c, c, alpha if alpha is not None else 1.
            raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
        # turn 2-D array into 1-D array
        if isinstance(c, np.ndarray):
            if c.ndim == 2 and c.shape[0] == 1:
                c = c.reshape(-1)
        # tuple color.
        if not np.iterable(c):
>           raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
E           ValueError: Invalid RGBA argument: None

/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:401: ValueError
____________ TestDecileplot.test_kind_decile_wise_gain_returns_axes ____________

self = <scikitplot.seaborn.tests.test__decile.TestDecileplot testMethod=test_kind_decile_wise_gain_returns_axes>

    def test_kind_decile_wise_gain_returns_axes(self):
>       ax = decileplot(_make_df(), x="x", y="y", kind="decile_wise_gain")
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

scikitplot/seaborn/tests/test__decile.py:338:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
scikitplot/seaborn/_decile.py:1823: in decileplot
    dt = p.plot_decileplot(
scikitplot/seaborn/_decile.py:1645: in plot_decileplot
    artist_kws = self._artist_kws(
scikitplot/seaborn/_decile.py:504: in _artist_kws
    kws["color"] = to_rgba(color, alpha)
                   ^^^^^^^^^^^^^^^^^^^^^
/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:317: in to_rgba
    rgba = _to_rgba_no_colorcycle(c, alpha)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

c = None, alpha = 1.0

    def _to_rgba_no_colorcycle(c, alpha=None):
        """
        Convert *c* to an RGBA color, with no support for color-cycle syntax.

        If *alpha* is given, force the alpha value of the returned RGBA tuple
        to *alpha*. Otherwise, the alpha value from *c* is used, if it has alpha
        information, or defaults to 1.

        *alpha* is ignored for the color value ``"none"`` (case-insensitive),
        which always maps to ``(0, 0, 0, 0)``.
        """
        if alpha is not None and not 0 <= alpha <= 1:
            raise ValueError("'alpha' must be between 0 and 1, inclusive")
        orig_c = c
        if c is np.ma.masked:
            return (0., 0., 0., 0.)
        if isinstance(c, str):
            if c.lower() == "none":
                return (0., 0., 0., 0.)
            # Named color.
            try:
                # This may turn c into a non-string, so we check again below.
                c = _colors_full_map[c]
            except KeyError:
                if len(orig_c) != 1:
                    try:
                        c = _colors_full_map[c.lower()]
                    except KeyError:
                        pass
        if isinstance(c, str):
            # hex color in #rrggbb format.
            match = re.match(r"\A#[a-fA-F0-9]{6}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1:3], c[3:5], c[5:7]])
                        + (alpha if alpha is not None else 1.,))
            # hex color in #rgb format, shorthand for #rrggbb.
            match = re.match(r"\A#[a-fA-F0-9]{3}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1]*2, c[2]*2, c[3]*2])
                        + (alpha if alpha is not None else 1.,))
            # hex color with alpha in #rrggbbaa format.
            match = re.match(r"\A#[a-fA-F0-9]{8}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1:3], c[3:5], c[5:7], c[7:9]]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # hex color with alpha in #rgba format, shorthand for #rrggbbaa.
            match = re.match(r"\A#[a-fA-F0-9]{4}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1]*2, c[2]*2, c[3]*2, c[4]*2]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # string gray.
            try:
                c = float(c)
            except ValueError:
                pass
            else:
                if not (0 <= c <= 1):
                    raise ValueError(
                        f"Invalid string grayscale value {orig_c!r}. "
                        f"Value must be within 0-1 range")
                return c, c, c, alpha if alpha is not None else 1.
            raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
        # turn 2-D array into 1-D array
        if isinstance(c, np.ndarray):
            if c.ndim == 2 and c.shape[0] == 1:
                c = c.reshape(-1)
        # tuple color.
        if not np.iterable(c):
>           raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
E           ValueError: Invalid RGBA argument: None

/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:401: ValueError
____________ TestDecileplot.test_kind_decile_wise_lift_returns_axes ____________

self = <scikitplot.seaborn.tests.test__decile.TestDecileplot testMethod=test_kind_decile_wise_lift_returns_axes>

    def test_kind_decile_wise_lift_returns_axes(self):
>       ax = decileplot(_make_df(), x="x", y="y", kind="decile_wise_lift")
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

scikitplot/seaborn/tests/test__decile.py:330:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
scikitplot/seaborn/_decile.py:1823: in decileplot
    dt = p.plot_decileplot(
scikitplot/seaborn/_decile.py:1645: in plot_decileplot
    artist_kws = self._artist_kws(
scikitplot/seaborn/_decile.py:504: in _artist_kws
    kws["color"] = to_rgba(color, alpha)
                   ^^^^^^^^^^^^^^^^^^^^^
/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:317: in to_rgba
    rgba = _to_rgba_no_colorcycle(c, alpha)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

c = None, alpha = 1.0

    def _to_rgba_no_colorcycle(c, alpha=None):
        """
        Convert *c* to an RGBA color, with no support for color-cycle syntax.

        If *alpha* is given, force the alpha value of the returned RGBA tuple
        to *alpha*. Otherwise, the alpha value from *c* is used, if it has alpha
        information, or defaults to 1.

        *alpha* is ignored for the color value ``"none"`` (case-insensitive),
        which always maps to ``(0, 0, 0, 0)``.
        """
        if alpha is not None and not 0 <= alpha <= 1:
            raise ValueError("'alpha' must be between 0 and 1, inclusive")
        orig_c = c
        if c is np.ma.masked:
            return (0., 0., 0., 0.)
        if isinstance(c, str):
            if c.lower() == "none":
                return (0., 0., 0., 0.)
            # Named color.
            try:
                # This may turn c into a non-string, so we check again below.
                c = _colors_full_map[c]
            except KeyError:
                if len(orig_c) != 1:
                    try:
                        c = _colors_full_map[c.lower()]
                    except KeyError:
                        pass
        if isinstance(c, str):
            # hex color in #rrggbb format.
            match = re.match(r"\A#[a-fA-F0-9]{6}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1:3], c[3:5], c[5:7]])
                        + (alpha if alpha is not None else 1.,))
            # hex color in #rgb format, shorthand for #rrggbb.
            match = re.match(r"\A#[a-fA-F0-9]{3}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1]*2, c[2]*2, c[3]*2])
                        + (alpha if alpha is not None else 1.,))
            # hex color with alpha in #rrggbbaa format.
            match = re.match(r"\A#[a-fA-F0-9]{8}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1:3], c[3:5], c[5:7], c[7:9]]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # hex color with alpha in #rgba format, shorthand for #rrggbbaa.
            match = re.match(r"\A#[a-fA-F0-9]{4}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1]*2, c[2]*2, c[3]*2, c[4]*2]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # string gray.
            try:
                c = float(c)
            except ValueError:
                pass
            else:
                if not (0 <= c <= 1):
                    raise ValueError(
                        f"Invalid string grayscale value {orig_c!r}. "
                        f"Value must be within 0-1 range")
                return c, c, c, alpha if alpha is not None else 1.
            raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
        # turn 2-D array into 1-D array
        if isinstance(c, np.ndarray):
            if c.ndim == 2 and c.shape[0] == 1:
                c = c.reshape(-1)
        # tuple color.
        if not np.iterable(c):
>           raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
E           ValueError: Invalid RGBA argument: None

/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:401: ValueError
_______________ TestDecileplot.test_kind_df_has_cumulative_lift ________________

self = <scikitplot.seaborn.tests.test__decile.TestDecileplot testMethod=test_kind_df_has_cumulative_lift>

    def test_kind_df_has_cumulative_lift(self):
>       dt = decileplot(_make_df(), x="x", y="y", kind="df")
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

scikitplot/seaborn/tests/test__decile.py:318:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
scikitplot/seaborn/_decile.py:1823: in decileplot
    dt = p.plot_decileplot(
scikitplot/seaborn/_decile.py:1645: in plot_decileplot
    artist_kws = self._artist_kws(
scikitplot/seaborn/_decile.py:504: in _artist_kws
    kws["color"] = to_rgba(color, alpha)
                   ^^^^^^^^^^^^^^^^^^^^^
/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:317: in to_rgba
    rgba = _to_rgba_no_colorcycle(c, alpha)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

c = None, alpha = 1.0

    def _to_rgba_no_colorcycle(c, alpha=None):
        """
        Convert *c* to an RGBA color, with no support for color-cycle syntax.

        If *alpha* is given, force the alpha value of the returned RGBA tuple
        to *alpha*. Otherwise, the alpha value from *c* is used, if it has alpha
        information, or defaults to 1.

        *alpha* is ignored for the color value ``"none"`` (case-insensitive),
        which always maps to ``(0, 0, 0, 0)``.
        """
        if alpha is not None and not 0 <= alpha <= 1:
            raise ValueError("'alpha' must be between 0 and 1, inclusive")
        orig_c = c
        if c is np.ma.masked:
            return (0., 0., 0., 0.)
        if isinstance(c, str):
            if c.lower() == "none":
                return (0., 0., 0., 0.)
            # Named color.
            try:
                # This may turn c into a non-string, so we check again below.
                c = _colors_full_map[c]
            except KeyError:
                if len(orig_c) != 1:
                    try:
                        c = _colors_full_map[c.lower()]
                    except KeyError:
                        pass
        if isinstance(c, str):
            # hex color in #rrggbb format.
            match = re.match(r"\A#[a-fA-F0-9]{6}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1:3], c[3:5], c[5:7]])
                        + (alpha if alpha is not None else 1.,))
            # hex color in #rgb format, shorthand for #rrggbb.
            match = re.match(r"\A#[a-fA-F0-9]{3}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1]*2, c[2]*2, c[3]*2])
                        + (alpha if alpha is not None else 1.,))
            # hex color with alpha in #rrggbbaa format.
            match = re.match(r"\A#[a-fA-F0-9]{8}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1:3], c[3:5], c[5:7], c[7:9]]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # hex color with alpha in #rgba format, shorthand for #rrggbbaa.
            match = re.match(r"\A#[a-fA-F0-9]{4}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1]*2, c[2]*2, c[3]*2, c[4]*2]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # string gray.
            try:
                c = float(c)
            except ValueError:
                pass
            else:
                if not (0 <= c <= 1):
                    raise ValueError(
                        f"Invalid string grayscale value {orig_c!r}. "
                        f"Value must be within 0-1 range")
                return c, c, c, alpha if alpha is not None else 1.
            raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
        # turn 2-D array into 1-D array
        if isinstance(c, np.ndarray):
            if c.ndim == 2 and c.shape[0] == 1:
                c = c.reshape(-1)
        # tuple color.
        if not np.iterable(c):
>           raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
E           ValueError: Invalid RGBA argument: None

/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:401: ValueError
__________________ TestDecileplot.test_kind_df_has_decile_col __________________

self = <scikitplot.seaborn.tests.test__decile.TestDecileplot testMethod=test_kind_df_has_decile_col>

    def test_kind_df_has_decile_col(self):
>       dt = decileplot(_make_df(), x="x", y="y", kind="df")
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

scikitplot/seaborn/tests/test__decile.py:314:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
scikitplot/seaborn/_decile.py:1823: in decileplot
    dt = p.plot_decileplot(
scikitplot/seaborn/_decile.py:1645: in plot_decileplot
    artist_kws = self._artist_kws(
scikitplot/seaborn/_decile.py:504: in _artist_kws
    kws["color"] = to_rgba(color, alpha)
                   ^^^^^^^^^^^^^^^^^^^^^
/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:317: in to_rgba
    rgba = _to_rgba_no_colorcycle(c, alpha)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

c = None, alpha = 1.0

    def _to_rgba_no_colorcycle(c, alpha=None):
        """
        Convert *c* to an RGBA color, with no support for color-cycle syntax.

        If *alpha* is given, force the alpha value of the returned RGBA tuple
        to *alpha*. Otherwise, the alpha value from *c* is used, if it has alpha
        information, or defaults to 1.

        *alpha* is ignored for the color value ``"none"`` (case-insensitive),
        which always maps to ``(0, 0, 0, 0)``.
        """
        if alpha is not None and not 0 <= alpha <= 1:
            raise ValueError("'alpha' must be between 0 and 1, inclusive")
        orig_c = c
        if c is np.ma.masked:
            return (0., 0., 0., 0.)
        if isinstance(c, str):
            if c.lower() == "none":
                return (0., 0., 0., 0.)
            # Named color.
            try:
                # This may turn c into a non-string, so we check again below.
                c = _colors_full_map[c]
            except KeyError:
                if len(orig_c) != 1:
                    try:
                        c = _colors_full_map[c.lower()]
                    except KeyError:
                        pass
        if isinstance(c, str):
            # hex color in #rrggbb format.
            match = re.match(r"\A#[a-fA-F0-9]{6}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1:3], c[3:5], c[5:7]])
                        + (alpha if alpha is not None else 1.,))
            # hex color in #rgb format, shorthand for #rrggbb.
            match = re.match(r"\A#[a-fA-F0-9]{3}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1]*2, c[2]*2, c[3]*2])
                        + (alpha if alpha is not None else 1.,))
            # hex color with alpha in #rrggbbaa format.
            match = re.match(r"\A#[a-fA-F0-9]{8}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1:3], c[3:5], c[5:7], c[7:9]]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # hex color with alpha in #rgba format, shorthand for #rrggbbaa.
            match = re.match(r"\A#[a-fA-F0-9]{4}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1]*2, c[2]*2, c[3]*2, c[4]*2]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # string gray.
            try:
                c = float(c)
            except ValueError:
                pass
            else:
                if not (0 <= c <= 1):
                    raise ValueError(
                        f"Invalid string grayscale value {orig_c!r}. "
                        f"Value must be within 0-1 range")
                return c, c, c, alpha if alpha is not None else 1.
            raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
        # turn 2-D array into 1-D array
        if isinstance(c, np.ndarray):
            if c.ndim == 2 and c.shape[0] == 1:
                c = c.reshape(-1)
        # tuple color.
        if not np.iterable(c):
>           raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
E           ValueError: Invalid RGBA argument: None

/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:401: ValueError
________________ TestDecileplot.test_kind_df_returns_dataframe _________________

self = <scikitplot.seaborn.tests.test__decile.TestDecileplot testMethod=test_kind_df_returns_dataframe>

    def test_kind_df_returns_dataframe(self):
>       result = decileplot(_make_df(), x="x", y="y", kind="df")
                 ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

scikitplot/seaborn/tests/test__decile.py:310:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
scikitplot/seaborn/_decile.py:1823: in decileplot
    dt = p.plot_decileplot(
scikitplot/seaborn/_decile.py:1645: in plot_decileplot
    artist_kws = self._artist_kws(
scikitplot/seaborn/_decile.py:504: in _artist_kws
    kws["color"] = to_rgba(color, alpha)
                   ^^^^^^^^^^^^^^^^^^^^^
/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:317: in to_rgba
    rgba = _to_rgba_no_colorcycle(c, alpha)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

c = None, alpha = 1.0

    def _to_rgba_no_colorcycle(c, alpha=None):
        """
        Convert *c* to an RGBA color, with no support for color-cycle syntax.

        If *alpha* is given, force the alpha value of the returned RGBA tuple
        to *alpha*. Otherwise, the alpha value from *c* is used, if it has alpha
        information, or defaults to 1.

        *alpha* is ignored for the color value ``"none"`` (case-insensitive),
        which always maps to ``(0, 0, 0, 0)``.
        """
        if alpha is not None and not 0 <= alpha <= 1:
            raise ValueError("'alpha' must be between 0 and 1, inclusive")
        orig_c = c
        if c is np.ma.masked:
            return (0., 0., 0., 0.)
        if isinstance(c, str):
            if c.lower() == "none":
                return (0., 0., 0., 0.)
            # Named color.
            try:
                # This may turn c into a non-string, so we check again below.
                c = _colors_full_map[c]
            except KeyError:
                if len(orig_c) != 1:
                    try:
                        c = _colors_full_map[c.lower()]
                    except KeyError:
                        pass
        if isinstance(c, str):
            # hex color in #rrggbb format.
            match = re.match(r"\A#[a-fA-F0-9]{6}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1:3], c[3:5], c[5:7]])
                        + (alpha if alpha is not None else 1.,))
            # hex color in #rgb format, shorthand for #rrggbb.
            match = re.match(r"\A#[a-fA-F0-9]{3}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1]*2, c[2]*2, c[3]*2])
                        + (alpha if alpha is not None else 1.,))
            # hex color with alpha in #rrggbbaa format.
            match = re.match(r"\A#[a-fA-F0-9]{8}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1:3], c[3:5], c[5:7], c[7:9]]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # hex color with alpha in #rgba format, shorthand for #rrggbbaa.
            match = re.match(r"\A#[a-fA-F0-9]{4}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1]*2, c[2]*2, c[3]*2, c[4]*2]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # string gray.
            try:
                c = float(c)
            except ValueError:
                pass
            else:
                if not (0 <= c <= 1):
                    raise ValueError(
                        f"Invalid string grayscale value {orig_c!r}. "
                        f"Value must be within 0-1 range")
                return c, c, c, alpha if alpha is not None else 1.
            raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
        # turn 2-D array into 1-D array
        if isinstance(c, np.ndarray):
            if c.ndim == 2 and c.shape[0] == 1:
                c = c.reshape(-1)
        # tuple color.
        if not np.iterable(c):
>           raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
E           ValueError: Invalid RGBA argument: None

/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:401: ValueError
______________ TestDecileplot.test_kind_ks_statistic_returns_axes ______________

self = <scikitplot.seaborn.tests.test__decile.TestDecileplot testMethod=test_kind_ks_statistic_returns_axes>

    def test_kind_ks_statistic_returns_axes(self):
>       ax = decileplot(_make_df(), x="x", y="y", kind="ks_statistic")
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

scikitplot/seaborn/tests/test__decile.py:346:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
scikitplot/seaborn/_decile.py:1823: in decileplot
    dt = p.plot_decileplot(
scikitplot/seaborn/_decile.py:1645: in plot_decileplot
    artist_kws = self._artist_kws(
scikitplot/seaborn/_decile.py:504: in _artist_kws
    kws["color"] = to_rgba(color, alpha)
                   ^^^^^^^^^^^^^^^^^^^^^
/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:317: in to_rgba
    rgba = _to_rgba_no_colorcycle(c, alpha)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

c = None, alpha = 1.0

    def _to_rgba_no_colorcycle(c, alpha=None):
        """
        Convert *c* to an RGBA color, with no support for color-cycle syntax.

        If *alpha* is given, force the alpha value of the returned RGBA tuple
        to *alpha*. Otherwise, the alpha value from *c* is used, if it has alpha
        information, or defaults to 1.

        *alpha* is ignored for the color value ``"none"`` (case-insensitive),
        which always maps to ``(0, 0, 0, 0)``.
        """
        if alpha is not None and not 0 <= alpha <= 1:
            raise ValueError("'alpha' must be between 0 and 1, inclusive")
        orig_c = c
        if c is np.ma.masked:
            return (0., 0., 0., 0.)
        if isinstance(c, str):
            if c.lower() == "none":
                return (0., 0., 0., 0.)
            # Named color.
            try:
                # This may turn c into a non-string, so we check again below.
                c = _colors_full_map[c]
            except KeyError:
                if len(orig_c) != 1:
                    try:
                        c = _colors_full_map[c.lower()]
                    except KeyError:
                        pass
        if isinstance(c, str):
            # hex color in #rrggbb format.
            match = re.match(r"\A#[a-fA-F0-9]{6}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1:3], c[3:5], c[5:7]])
                        + (alpha if alpha is not None else 1.,))
            # hex color in #rgb format, shorthand for #rrggbb.
            match = re.match(r"\A#[a-fA-F0-9]{3}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1]*2, c[2]*2, c[3]*2])
                        + (alpha if alpha is not None else 1.,))
            # hex color with alpha in #rrggbbaa format.
            match = re.match(r"\A#[a-fA-F0-9]{8}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1:3], c[3:5], c[5:7], c[7:9]]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # hex color with alpha in #rgba format, shorthand for #rrggbbaa.
            match = re.match(r"\A#[a-fA-F0-9]{4}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1]*2, c[2]*2, c[3]*2, c[4]*2]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # string gray.
            try:
                c = float(c)
            except ValueError:
                pass
            else:
                if not (0 <= c <= 1):
                    raise ValueError(
                        f"Invalid string grayscale value {orig_c!r}. "
                        f"Value must be within 0-1 range")
                return c, c, c, alpha if alpha is not None else 1.
            raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
        # turn 2-D array into 1-D array
        if isinstance(c, np.ndarray):
            if c.ndim == 2 and c.shape[0] == 1:
                c = c.reshape(-1)
        # tuple color.
        if not np.iterable(c):
>           raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
E           ValueError: Invalid RGBA argument: None

/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:401: ValueError
_______________ TestDecileplot.test_kind_none_defaults_to_report _______________

self = <scikitplot.seaborn.tests.test__decile.TestDecileplot testMethod=test_kind_none_defaults_to_report>

    def test_kind_none_defaults_to_report(self):
>       ax = decileplot(_make_df(), x="x", y="y")
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

scikitplot/seaborn/tests/test__decile.py:377:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
scikitplot/seaborn/_decile.py:1823: in decileplot
    dt = p.plot_decileplot(
scikitplot/seaborn/_decile.py:1645: in plot_decileplot
    artist_kws = self._artist_kws(
scikitplot/seaborn/_decile.py:504: in _artist_kws
    kws["color"] = to_rgba(color, alpha)
                   ^^^^^^^^^^^^^^^^^^^^^
/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:317: in to_rgba
    rgba = _to_rgba_no_colorcycle(c, alpha)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

c = None, alpha = 1.0

    def _to_rgba_no_colorcycle(c, alpha=None):
        """
        Convert *c* to an RGBA color, with no support for color-cycle syntax.

        If *alpha* is given, force the alpha value of the returned RGBA tuple
        to *alpha*. Otherwise, the alpha value from *c* is used, if it has alpha
        information, or defaults to 1.

        *alpha* is ignored for the color value ``"none"`` (case-insensitive),
        which always maps to ``(0, 0, 0, 0)``.
        """
        if alpha is not None and not 0 <= alpha <= 1:
            raise ValueError("'alpha' must be between 0 and 1, inclusive")
        orig_c = c
        if c is np.ma.masked:
            return (0., 0., 0., 0.)
        if isinstance(c, str):
            if c.lower() == "none":
                return (0., 0., 0., 0.)
            # Named color.
            try:
                # This may turn c into a non-string, so we check again below.
                c = _colors_full_map[c]
            except KeyError:
                if len(orig_c) != 1:
                    try:
                        c = _colors_full_map[c.lower()]
                    except KeyError:
                        pass
        if isinstance(c, str):
            # hex color in #rrggbb format.
            match = re.match(r"\A#[a-fA-F0-9]{6}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1:3], c[3:5], c[5:7]])
                        + (alpha if alpha is not None else 1.,))
            # hex color in #rgb format, shorthand for #rrggbb.
            match = re.match(r"\A#[a-fA-F0-9]{3}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1]*2, c[2]*2, c[3]*2])
                        + (alpha if alpha is not None else 1.,))
            # hex color with alpha in #rrggbbaa format.
            match = re.match(r"\A#[a-fA-F0-9]{8}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1:3], c[3:5], c[5:7], c[7:9]]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # hex color with alpha in #rgba format, shorthand for #rrggbbaa.
            match = re.match(r"\A#[a-fA-F0-9]{4}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1]*2, c[2]*2, c[3]*2, c[4]*2]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # string gray.
            try:
                c = float(c)
            except ValueError:
                pass
            else:
                if not (0 <= c <= 1):
                    raise ValueError(
                        f"Invalid string grayscale value {orig_c!r}. "
                        f"Value must be within 0-1 range")
                return c, c, c, alpha if alpha is not None else 1.
            raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
        # turn 2-D array into 1-D array
        if isinstance(c, np.ndarray):
            if c.ndim == 2 and c.shape[0] == 1:
                c = c.reshape(-1)
        # tuple color.
        if not np.iterable(c):
>           raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
E           ValueError: Invalid RGBA argument: None

/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:401: ValueError
_________________ TestDecileplot.test_kind_report_returns_axes _________________

self = <scikitplot.seaborn.tests.test__decile.TestDecileplot testMethod=test_kind_report_returns_axes>

    def test_kind_report_returns_axes(self):
>       ax = decileplot(_make_df(), x="x", y="y", kind="report")
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

scikitplot/seaborn/tests/test__decile.py:322:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
scikitplot/seaborn/_decile.py:1823: in decileplot
    dt = p.plot_decileplot(
scikitplot/seaborn/_decile.py:1645: in plot_decileplot
    artist_kws = self._artist_kws(
scikitplot/seaborn/_decile.py:504: in _artist_kws
    kws["color"] = to_rgba(color, alpha)
                   ^^^^^^^^^^^^^^^^^^^^^
/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:317: in to_rgba
    rgba = _to_rgba_no_colorcycle(c, alpha)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

c = None, alpha = 1.0

    def _to_rgba_no_colorcycle(c, alpha=None):
        """
        Convert *c* to an RGBA color, with no support for color-cycle syntax.

        If *alpha* is given, force the alpha value of the returned RGBA tuple
        to *alpha*. Otherwise, the alpha value from *c* is used, if it has alpha
        information, or defaults to 1.

        *alpha* is ignored for the color value ``"none"`` (case-insensitive),
        which always maps to ``(0, 0, 0, 0)``.
        """
        if alpha is not None and not 0 <= alpha <= 1:
            raise ValueError("'alpha' must be between 0 and 1, inclusive")
        orig_c = c
        if c is np.ma.masked:
            return (0., 0., 0., 0.)
        if isinstance(c, str):
            if c.lower() == "none":
                return (0., 0., 0., 0.)
            # Named color.
            try:
                # This may turn c into a non-string, so we check again below.
                c = _colors_full_map[c]
            except KeyError:
                if len(orig_c) != 1:
                    try:
                        c = _colors_full_map[c.lower()]
                    except KeyError:
                        pass
        if isinstance(c, str):
            # hex color in #rrggbb format.
            match = re.match(r"\A#[a-fA-F0-9]{6}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1:3], c[3:5], c[5:7]])
                        + (alpha if alpha is not None else 1.,))
            # hex color in #rgb format, shorthand for #rrggbb.
            match = re.match(r"\A#[a-fA-F0-9]{3}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1]*2, c[2]*2, c[3]*2])
                        + (alpha if alpha is not None else 1.,))
            # hex color with alpha in #rrggbbaa format.
            match = re.match(r"\A#[a-fA-F0-9]{8}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1:3], c[3:5], c[5:7], c[7:9]]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # hex color with alpha in #rgba format, shorthand for #rrggbbaa.
            match = re.match(r"\A#[a-fA-F0-9]{4}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1]*2, c[2]*2, c[3]*2, c[4]*2]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # string gray.
            try:
                c = float(c)
            except ValueError:
                pass
            else:
                if not (0 <= c <= 1):
                    raise ValueError(
                        f"Invalid string grayscale value {orig_c!r}. "
                        f"Value must be within 0-1 range")
                return c, c, c, alpha if alpha is not None else 1.
            raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
        # turn 2-D array into 1-D array
        if isinstance(c, np.ndarray):
            if c.ndim == 2 and c.shape[0] == 1:
                c = c.reshape(-1)
        # tuple color.
        if not np.iterable(c):
>           raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
E           ValueError: Invalid RGBA argument: None

/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:401: ValueError
_______________________ TestDecileplot.test_n_deciles_5 ________________________

self = <scikitplot.seaborn.tests.test__decile.TestDecileplot testMethod=test_n_deciles_5>

    def test_n_deciles_5(self):
>       dt = decileplot(_make_df(500), x="x", y="y", kind="df", n_deciles=5)
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

scikitplot/seaborn/tests/test__decile.py:369:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
scikitplot/seaborn/_decile.py:1823: in decileplot
    dt = p.plot_decileplot(
scikitplot/seaborn/_decile.py:1645: in plot_decileplot
    artist_kws = self._artist_kws(
scikitplot/seaborn/_decile.py:504: in _artist_kws
    kws["color"] = to_rgba(color, alpha)
                   ^^^^^^^^^^^^^^^^^^^^^
/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:317: in to_rgba
    rgba = _to_rgba_no_colorcycle(c, alpha)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

c = None, alpha = 1.0

    def _to_rgba_no_colorcycle(c, alpha=None):
        """
        Convert *c* to an RGBA color, with no support for color-cycle syntax.

        If *alpha* is given, force the alpha value of the returned RGBA tuple
        to *alpha*. Otherwise, the alpha value from *c* is used, if it has alpha
        information, or defaults to 1.

        *alpha* is ignored for the color value ``"none"`` (case-insensitive),
        which always maps to ``(0, 0, 0, 0)``.
        """
        if alpha is not None and not 0 <= alpha <= 1:
            raise ValueError("'alpha' must be between 0 and 1, inclusive")
        orig_c = c
        if c is np.ma.masked:
            return (0., 0., 0., 0.)
        if isinstance(c, str):
            if c.lower() == "none":
                return (0., 0., 0., 0.)
            # Named color.
            try:
                # This may turn c into a non-string, so we check again below.
                c = _colors_full_map[c]
            except KeyError:
                if len(orig_c) != 1:
                    try:
                        c = _colors_full_map[c.lower()]
                    except KeyError:
                        pass
        if isinstance(c, str):
            # hex color in #rrggbb format.
            match = re.match(r"\A#[a-fA-F0-9]{6}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1:3], c[3:5], c[5:7]])
                        + (alpha if alpha is not None else 1.,))
            # hex color in #rgb format, shorthand for #rrggbb.
            match = re.match(r"\A#[a-fA-F0-9]{3}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1]*2, c[2]*2, c[3]*2])
                        + (alpha if alpha is not None else 1.,))
            # hex color with alpha in #rrggbbaa format.
            match = re.match(r"\A#[a-fA-F0-9]{8}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1:3], c[3:5], c[5:7], c[7:9]]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # hex color with alpha in #rgba format, shorthand for #rrggbbaa.
            match = re.match(r"\A#[a-fA-F0-9]{4}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1]*2, c[2]*2, c[3]*2, c[4]*2]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # string gray.
            try:
                c = float(c)
            except ValueError:
                pass
            else:
                if not (0 <= c <= 1):
                    raise ValueError(
                        f"Invalid string grayscale value {orig_c!r}. "
                        f"Value must be within 0-1 range")
                return c, c, c, alpha if alpha is not None else 1.
            raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
        # turn 2-D array into 1-D array
        if isinstance(c, np.ndarray):
            if c.ndim == 2 and c.shape[0] == 1:
                c = c.reshape(-1)
        # tuple color.
        if not np.iterable(c):
>           raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
E           ValueError: Invalid RGBA argument: None

/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:401: ValueError
____________________ TestModelplot.test_ax_param_respected _____________________

self = <scikitplot.seaborn.tests.test__model.TestModelplot testMethod=test_ax_param_respected>

    def test_ax_param_respected(self):
        fig, ax = plt.subplots()
>       result = modelplot(_make_df(), x="x", y="y", ax=ax)
                 ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

scikitplot/seaborn/tests/test__model.py:238:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
scikitplot/seaborn/_model.py:871: in modelplot
    p.plot_modelplot(
scikitplot/seaborn/_model.py:757: in plot_modelplot
    artist_kws = self._artist_kws(
scikitplot/seaborn/_model.py:308: in _artist_kws
    kws["color"] = to_rgba(color, alpha)
                   ^^^^^^^^^^^^^^^^^^^^^
/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:317: in to_rgba
    rgba = _to_rgba_no_colorcycle(c, alpha)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

c = None, alpha = 1.0

    def _to_rgba_no_colorcycle(c, alpha=None):
        """
        Convert *c* to an RGBA color, with no support for color-cycle syntax.

        If *alpha* is given, force the alpha value of the returned RGBA tuple
        to *alpha*. Otherwise, the alpha value from *c* is used, if it has alpha
        information, or defaults to 1.

        *alpha* is ignored for the color value ``"none"`` (case-insensitive),
        which always maps to ``(0, 0, 0, 0)``.
        """
        if alpha is not None and not 0 <= alpha <= 1:
            raise ValueError("'alpha' must be between 0 and 1, inclusive")
        orig_c = c
        if c is np.ma.masked:
            return (0., 0., 0., 0.)
        if isinstance(c, str):
            if c.lower() == "none":
                return (0., 0., 0., 0.)
            # Named color.
            try:
                # This may turn c into a non-string, so we check again below.
                c = _colors_full_map[c]
            except KeyError:
                if len(orig_c) != 1:
                    try:
                        c = _colors_full_map[c.lower()]
                    except KeyError:
                        pass
        if isinstance(c, str):
            # hex color in #rrggbb format.
            match = re.match(r"\A#[a-fA-F0-9]{6}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1:3], c[3:5], c[5:7]])
                        + (alpha if alpha is not None else 1.,))
            # hex color in #rgb format, shorthand for #rrggbb.
            match = re.match(r"\A#[a-fA-F0-9]{3}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1]*2, c[2]*2, c[3]*2])
                        + (alpha if alpha is not None else 1.,))
            # hex color with alpha in #rrggbbaa format.
            match = re.match(r"\A#[a-fA-F0-9]{8}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1:3], c[3:5], c[5:7], c[7:9]]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # hex color with alpha in #rgba format, shorthand for #rrggbbaa.
            match = re.match(r"\A#[a-fA-F0-9]{4}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1]*2, c[2]*2, c[3]*2, c[4]*2]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # string gray.
            try:
                c = float(c)
            except ValueError:
                pass
            else:
                if not (0 <= c <= 1):
                    raise ValueError(
                        f"Invalid string grayscale value {orig_c!r}. "
                        f"Value must be within 0-1 range")
                return c, c, c, alpha if alpha is not None else 1.
            raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
        # turn 2-D array into 1-D array
        if isinstance(c, np.ndarray):
            if c.ndim == 2 and c.shape[0] == 1:
                c = c.reshape(-1)
        # tuple color.
        if not np.iterable(c):
>           raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
E           ValueError: Invalid RGBA argument: None

/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:401: ValueError
___________________ TestModelplot.test_kind_case_insensitive ___________________

self = <scikitplot.seaborn.tests.test__model.TestModelplot testMethod=test_kind_case_insensitive>

    def test_kind_case_insensitive(self):
>       ax = modelplot(_make_df(), x="x", y="y", kind="Feature_Importances")
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

scikitplot/seaborn/tests/test__model.py:252:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
scikitplot/seaborn/_model.py:871: in modelplot
    p.plot_modelplot(
scikitplot/seaborn/_model.py:757: in plot_modelplot
    artist_kws = self._artist_kws(
scikitplot/seaborn/_model.py:308: in _artist_kws
    kws["color"] = to_rgba(color, alpha)
                   ^^^^^^^^^^^^^^^^^^^^^
/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:317: in to_rgba
    rgba = _to_rgba_no_colorcycle(c, alpha)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

c = None, alpha = 1.0

    def _to_rgba_no_colorcycle(c, alpha=None):
        """
        Convert *c* to an RGBA color, with no support for color-cycle syntax.

        If *alpha* is given, force the alpha value of the returned RGBA tuple
        to *alpha*. Otherwise, the alpha value from *c* is used, if it has alpha
        information, or defaults to 1.

        *alpha* is ignored for the color value ``"none"`` (case-insensitive),
        which always maps to ``(0, 0, 0, 0)``.
        """
        if alpha is not None and not 0 <= alpha <= 1:
            raise ValueError("'alpha' must be between 0 and 1, inclusive")
        orig_c = c
        if c is np.ma.masked:
            return (0., 0., 0., 0.)
        if isinstance(c, str):
            if c.lower() == "none":
                return (0., 0., 0., 0.)
            # Named color.
            try:
                # This may turn c into a non-string, so we check again below.
                c = _colors_full_map[c]
            except KeyError:
                if len(orig_c) != 1:
                    try:
                        c = _colors_full_map[c.lower()]
                    except KeyError:
                        pass
        if isinstance(c, str):
            # hex color in #rrggbb format.
            match = re.match(r"\A#[a-fA-F0-9]{6}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1:3], c[3:5], c[5:7]])
                        + (alpha if alpha is not None else 1.,))
            # hex color in #rgb format, shorthand for #rrggbb.
            match = re.match(r"\A#[a-fA-F0-9]{3}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1]*2, c[2]*2, c[3]*2])
                        + (alpha if alpha is not None else 1.,))
            # hex color with alpha in #rrggbbaa format.
            match = re.match(r"\A#[a-fA-F0-9]{8}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1:3], c[3:5], c[5:7], c[7:9]]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # hex color with alpha in #rgba format, shorthand for #rrggbbaa.
            match = re.match(r"\A#[a-fA-F0-9]{4}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1]*2, c[2]*2, c[3]*2, c[4]*2]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # string gray.
            try:
                c = float(c)
            except ValueError:
                pass
            else:
                if not (0 <= c <= 1):
                    raise ValueError(
                        f"Invalid string grayscale value {orig_c!r}. "
                        f"Value must be within 0-1 range")
                return c, c, c, alpha if alpha is not None else 1.
            raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
        # turn 2-D array into 1-D array
        if isinstance(c, np.ndarray):
            if c.ndim == 2 and c.shape[0] == 1:
                c = c.reshape(-1)
        # tuple color.
        if not np.iterable(c):
>           raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
E           ValueError: Invalid RGBA argument: None

/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:401: ValueError
_________________ TestModelplot.test_kind_none_does_not_raise __________________

self = <scikitplot.seaborn.tests.test__model.TestModelplot testMethod=test_kind_none_does_not_raise>

    def test_kind_none_does_not_raise(self):
>       ax = modelplot(_make_df(), x="x", y="y")
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

scikitplot/seaborn/tests/test__model.py:224:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
scikitplot/seaborn/_model.py:871: in modelplot
    p.plot_modelplot(
scikitplot/seaborn/_model.py:757: in plot_modelplot
    artist_kws = self._artist_kws(
scikitplot/seaborn/_model.py:308: in _artist_kws
    kws["color"] = to_rgba(color, alpha)
                   ^^^^^^^^^^^^^^^^^^^^^
/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:317: in to_rgba
    rgba = _to_rgba_no_colorcycle(c, alpha)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

c = None, alpha = 1.0

    def _to_rgba_no_colorcycle(c, alpha=None):
        """
        Convert *c* to an RGBA color, with no support for color-cycle syntax.

        If *alpha* is given, force the alpha value of the returned RGBA tuple
        to *alpha*. Otherwise, the alpha value from *c* is used, if it has alpha
        information, or defaults to 1.

        *alpha* is ignored for the color value ``"none"`` (case-insensitive),
        which always maps to ``(0, 0, 0, 0)``.
        """
        if alpha is not None and not 0 <= alpha <= 1:
            raise ValueError("'alpha' must be between 0 and 1, inclusive")
        orig_c = c
        if c is np.ma.masked:
            return (0., 0., 0., 0.)
        if isinstance(c, str):
            if c.lower() == "none":
                return (0., 0., 0., 0.)
            # Named color.
            try:
                # This may turn c into a non-string, so we check again below.
                c = _colors_full_map[c]
            except KeyError:
                if len(orig_c) != 1:
                    try:
                        c = _colors_full_map[c.lower()]
                    except KeyError:
                        pass
        if isinstance(c, str):
            # hex color in #rrggbb format.
            match = re.match(r"\A#[a-fA-F0-9]{6}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1:3], c[3:5], c[5:7]])
                        + (alpha if alpha is not None else 1.,))
            # hex color in #rgb format, shorthand for #rrggbb.
            match = re.match(r"\A#[a-fA-F0-9]{3}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1]*2, c[2]*2, c[3]*2])
                        + (alpha if alpha is not None else 1.,))
            # hex color with alpha in #rrggbbaa format.
            match = re.match(r"\A#[a-fA-F0-9]{8}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1:3], c[3:5], c[5:7], c[7:9]]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # hex color with alpha in #rgba format, shorthand for #rrggbbaa.
            match = re.match(r"\A#[a-fA-F0-9]{4}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1]*2, c[2]*2, c[3]*2, c[4]*2]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # string gray.
            try:
                c = float(c)
            except ValueError:
                pass
            else:
                if not (0 <= c <= 1):
                    raise ValueError(
                        f"Invalid string grayscale value {orig_c!r}. "
                        f"Value must be within 0-1 range")
                return c, c, c, alpha if alpha is not None else 1.
            raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
        # turn 2-D array into 1-D array
        if isinstance(c, np.ndarray):
            if c.ndim == 2 and c.shape[0] == 1:
                c = c.reshape(-1)
        # tuple color.
        if not np.iterable(c):
>           raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
E           ValueError: Invalid RGBA argument: None

/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:401: ValueError
______________________ TestModelplot.test_minimum_dataset ______________________

self = <scikitplot.seaborn.tests.test__model.TestModelplot testMethod=test_minimum_dataset>

    def test_minimum_dataset(self):
        df = pd.DataFrame({"x": [0, 1], "y": [0.0, 1.0]})
>       ax = modelplot(df, x="x", y="y")
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^

scikitplot/seaborn/tests/test__model.py:257:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
scikitplot/seaborn/_model.py:871: in modelplot
    p.plot_modelplot(
scikitplot/seaborn/_model.py:757: in plot_modelplot
    artist_kws = self._artist_kws(
scikitplot/seaborn/_model.py:308: in _artist_kws
    kws["color"] = to_rgba(color, alpha)
                   ^^^^^^^^^^^^^^^^^^^^^
/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:317: in to_rgba
    rgba = _to_rgba_no_colorcycle(c, alpha)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

c = None, alpha = 1.0

    def _to_rgba_no_colorcycle(c, alpha=None):
        """
        Convert *c* to an RGBA color, with no support for color-cycle syntax.

        If *alpha* is given, force the alpha value of the returned RGBA tuple
        to *alpha*. Otherwise, the alpha value from *c* is used, if it has alpha
        information, or defaults to 1.

        *alpha* is ignored for the color value ``"none"`` (case-insensitive),
        which always maps to ``(0, 0, 0, 0)``.
        """
        if alpha is not None and not 0 <= alpha <= 1:
            raise ValueError("'alpha' must be between 0 and 1, inclusive")
        orig_c = c
        if c is np.ma.masked:
            return (0., 0., 0., 0.)
        if isinstance(c, str):
            if c.lower() == "none":
                return (0., 0., 0., 0.)
            # Named color.
            try:
                # This may turn c into a non-string, so we check again below.
                c = _colors_full_map[c]
            except KeyError:
                if len(orig_c) != 1:
                    try:
                        c = _colors_full_map[c.lower()]
                    except KeyError:
                        pass
        if isinstance(c, str):
            # hex color in #rrggbb format.
            match = re.match(r"\A#[a-fA-F0-9]{6}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1:3], c[3:5], c[5:7]])
                        + (alpha if alpha is not None else 1.,))
            # hex color in #rgb format, shorthand for #rrggbb.
            match = re.match(r"\A#[a-fA-F0-9]{3}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1]*2, c[2]*2, c[3]*2])
                        + (alpha if alpha is not None else 1.,))
            # hex color with alpha in #rrggbbaa format.
            match = re.match(r"\A#[a-fA-F0-9]{8}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1:3], c[3:5], c[5:7], c[7:9]]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # hex color with alpha in #rgba format, shorthand for #rrggbbaa.
            match = re.match(r"\A#[a-fA-F0-9]{4}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1]*2, c[2]*2, c[3]*2, c[4]*2]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # string gray.
            try:
                c = float(c)
            except ValueError:
                pass
            else:
                if not (0 <= c <= 1):
                    raise ValueError(
                        f"Invalid string grayscale value {orig_c!r}. "
                        f"Value must be within 0-1 range")
                return c, c, c, alpha if alpha is not None else 1.
            raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
        # turn 2-D array into 1-D array
        if isinstance(c, np.ndarray):
            if c.ndim == 2 and c.shape[0] == 1:
                c = c.reshape(-1)
        # tuple color.
        if not np.iterable(c):
>           raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
E           ValueError: Invalid RGBA argument: None

/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:401: ValueError
____________________ TestModelplot.test_multiclass_no_crash ____________________

self = <scikitplot.seaborn.tests.test__model.TestModelplot testMethod=test_multiclass_no_crash>

    def test_multiclass_no_crash(self):
        rng = np.random.default_rng(5)
        n = 60
        df = pd.DataFrame({
            "x": rng.integers(0, 3, n).astype(int),
            "y": rng.integers(0, 3, n).astype(float),
        })
>       ax = modelplot(df, x="x", y="y")
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^

scikitplot/seaborn/tests/test__model.py:248:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
scikitplot/seaborn/_model.py:871: in modelplot
    p.plot_modelplot(
scikitplot/seaborn/_model.py:757: in plot_modelplot
    artist_kws = self._artist_kws(
scikitplot/seaborn/_model.py:308: in _artist_kws
    kws["color"] = to_rgba(color, alpha)
                   ^^^^^^^^^^^^^^^^^^^^^
/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:317: in to_rgba
    rgba = _to_rgba_no_colorcycle(c, alpha)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

c = None, alpha = 1.0

    def _to_rgba_no_colorcycle(c, alpha=None):
        """
        Convert *c* to an RGBA color, with no support for color-cycle syntax.

        If *alpha* is given, force the alpha value of the returned RGBA tuple
        to *alpha*. Otherwise, the alpha value from *c* is used, if it has alpha
        information, or defaults to 1.

        *alpha* is ignored for the color value ``"none"`` (case-insensitive),
        which always maps to ``(0, 0, 0, 0)``.
        """
        if alpha is not None and not 0 <= alpha <= 1:
            raise ValueError("'alpha' must be between 0 and 1, inclusive")
        orig_c = c
        if c is np.ma.masked:
            return (0., 0., 0., 0.)
        if isinstance(c, str):
            if c.lower() == "none":
                return (0., 0., 0., 0.)
            # Named color.
            try:
                # This may turn c into a non-string, so we check again below.
                c = _colors_full_map[c]
            except KeyError:
                if len(orig_c) != 1:
                    try:
                        c = _colors_full_map[c.lower()]
                    except KeyError:
                        pass
        if isinstance(c, str):
            # hex color in #rrggbb format.
            match = re.match(r"\A#[a-fA-F0-9]{6}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1:3], c[3:5], c[5:7]])
                        + (alpha if alpha is not None else 1.,))
            # hex color in #rgb format, shorthand for #rrggbb.
            match = re.match(r"\A#[a-fA-F0-9]{3}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1]*2, c[2]*2, c[3]*2])
                        + (alpha if alpha is not None else 1.,))
            # hex color with alpha in #rrggbbaa format.
            match = re.match(r"\A#[a-fA-F0-9]{8}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1:3], c[3:5], c[5:7], c[7:9]]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # hex color with alpha in #rgba format, shorthand for #rrggbbaa.
            match = re.match(r"\A#[a-fA-F0-9]{4}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1]*2, c[2]*2, c[3]*2, c[4]*2]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # string gray.
            try:
                c = float(c)
            except ValueError:
                pass
            else:
                if not (0 <= c <= 1):
                    raise ValueError(
                        f"Invalid string grayscale value {orig_c!r}. "
                        f"Value must be within 0-1 range")
                return c, c, c, alpha if alpha is not None else 1.
            raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
        # turn 2-D array into 1-D array
        if isinstance(c, np.ndarray):
            if c.ndim == 2 and c.shape[0] == 1:
                c = c.reshape(-1)
        # tuple color.
        if not np.iterable(c):
>           raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
E           ValueError: Invalid RGBA argument: None

/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:401: ValueError
_______________________ TestModelplot.test_returns_axes ________________________

self = <scikitplot.seaborn.tests.test__model.TestModelplot testMethod=test_returns_axes>

    def test_returns_axes(self):
>       ax = modelplot(_make_df(), x="x", y="y", kind="feature_importances")
             ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^

scikitplot/seaborn/tests/test__model.py:220:
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _
scikitplot/seaborn/_model.py:871: in modelplot
    p.plot_modelplot(
scikitplot/seaborn/_model.py:757: in plot_modelplot
    artist_kws = self._artist_kws(
scikitplot/seaborn/_model.py:308: in _artist_kws
    kws["color"] = to_rgba(color, alpha)
                   ^^^^^^^^^^^^^^^^^^^^^
/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:317: in to_rgba
    rgba = _to_rgba_no_colorcycle(c, alpha)
           ^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^^
_ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _ _

c = None, alpha = 1.0

    def _to_rgba_no_colorcycle(c, alpha=None):
        """
        Convert *c* to an RGBA color, with no support for color-cycle syntax.

        If *alpha* is given, force the alpha value of the returned RGBA tuple
        to *alpha*. Otherwise, the alpha value from *c* is used, if it has alpha
        information, or defaults to 1.

        *alpha* is ignored for the color value ``"none"`` (case-insensitive),
        which always maps to ``(0, 0, 0, 0)``.
        """
        if alpha is not None and not 0 <= alpha <= 1:
            raise ValueError("'alpha' must be between 0 and 1, inclusive")
        orig_c = c
        if c is np.ma.masked:
            return (0., 0., 0., 0.)
        if isinstance(c, str):
            if c.lower() == "none":
                return (0., 0., 0., 0.)
            # Named color.
            try:
                # This may turn c into a non-string, so we check again below.
                c = _colors_full_map[c]
            except KeyError:
                if len(orig_c) != 1:
                    try:
                        c = _colors_full_map[c.lower()]
                    except KeyError:
                        pass
        if isinstance(c, str):
            # hex color in #rrggbb format.
            match = re.match(r"\A#[a-fA-F0-9]{6}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1:3], c[3:5], c[5:7]])
                        + (alpha if alpha is not None else 1.,))
            # hex color in #rgb format, shorthand for #rrggbb.
            match = re.match(r"\A#[a-fA-F0-9]{3}\Z", c)
            if match:
                return (tuple(int(n, 16) / 255
                              for n in [c[1]*2, c[2]*2, c[3]*2])
                        + (alpha if alpha is not None else 1.,))
            # hex color with alpha in #rrggbbaa format.
            match = re.match(r"\A#[a-fA-F0-9]{8}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1:3], c[3:5], c[5:7], c[7:9]]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # hex color with alpha in #rgba format, shorthand for #rrggbbaa.
            match = re.match(r"\A#[a-fA-F0-9]{4}\Z", c)
            if match:
                color = [int(n, 16) / 255
                         for n in [c[1]*2, c[2]*2, c[3]*2, c[4]*2]]
                if alpha is not None:
                    color[-1] = alpha
                return tuple(color)
            # string gray.
            try:
                c = float(c)
            except ValueError:
                pass
            else:
                if not (0 <= c <= 1):
                    raise ValueError(
                        f"Invalid string grayscale value {orig_c!r}. "
                        f"Value must be within 0-1 range")
                return c, c, c, alpha if alpha is not None else 1.
            raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
        # turn 2-D array into 1-D array
        if isinstance(c, np.ndarray):
            if c.ndim == 2 and c.shape[0] == 1:
                c = c.reshape(-1)
        # tuple color.
        if not np.iterable(c):
>           raise ValueError(f"Invalid RGBA argument: {orig_c!r}")
E           ValueError: Invalid RGBA argument: None

/opt/pyvenv/lib/python3.13/site-packages/matplotlib/colors.py:401: ValueError
=========================== short test summary info ============================
FAILED scikitplot/seaborn/tests/test__auc.py::TestAucplot::test_accepts_numpy_arrays - ValueError: Invalid RGBA argument: None
FAILED scikitplot/seaborn/tests/test__auc.py::TestAucplot::test_accepts_pandas_series - ValueError: Invalid RGBA argument: None
FAILED scikitplot/seaborn/tests/test__auc.py::TestAucplot::test_all_positive_no_crash - ValueError: Invalid RGBA argument: None
FAILED scikitplot/seaborn/tests/test__auc.py::TestAucplot::test_auc_in_legend_text_pr - ValueError: Invalid RGBA argument: None
FAILED scikitplot/seaborn/tests/test__auc.py::TestAucplot::test_auc_in_legend_text_roc - ValueError: Invalid RGBA argument: None
FAILED scikitplot/seaborn/tests/test__auc.py::TestAucplot::test_ax_param_respected - ValueError: Invalid RGBA argument: None
FAILED scikitplot/seaborn/tests/test__auc.py::TestAucplot::test_fill_true_does_not_crash - ValueError: Invalid RGBA argument: None
FAILED scikitplot/seaborn/tests/test__auc.py::TestAucplot::test_kind_none_defaults_to_roc - ValueError: Invalid RGBA argument: None
FAILED scikitplot/seaborn/tests/test__auc.py::TestAucplot::test_kind_uppercase_accepted - ValueError: Invalid RGBA argument: None
FAILED scikitplot/seaborn/tests/test__auc.py::TestAucplot::test_legend_false_no_legend - ValueError: Invalid RGBA argument: None
FAILED scikitplot/seaborn/tests/test__auc.py::TestAucplot::test_minimum_two_samples - ValueError: Invalid RGBA argument: None
FAILED scikitplot/seaborn/tests/test__auc.py::TestAucplot::test_pr_xlabel_contains_recall - ValueError: Invalid RGBA argument: None
FAILED scikitplot/seaborn/tests/test__auc.py::TestAucplot::test_pr_ylabel_contains_precision - ValueError: Invalid RGBA argument: None
FAILED scikitplot/seaborn/tests/test__auc.py::TestAucplot::test_returns_axes_pr - ValueError: Invalid RGBA argument: None
FAILED scikitplot/seaborn/tests/test__auc.py::TestAucplot::test_returns_axes_roc - ValueError: Invalid RGBA argument: None
FAILED scikitplot/seaborn/tests/test__auc.py::TestAucplot::test_roc_xlabel_contains_fpr - ValueError: Invalid RGBA argument: None
FAILED scikitplot/seaborn/tests/test__auc.py::TestAucplot::test_roc_ylabel_contains_tpr - ValueError: Invalid RGBA argument: None
FAILED scikitplot/seaborn/tests/test__confusion_matrix.py::TestEvalplot::test_allow_probs_binary - ValueError: Invalid RGBA argument: None
FAILED scikitplot/seaborn/tests/test__confusion_matrix.py::TestEvalplot::test_allow_probs_multiclass_emits_warning - ValueError: Invalid RGBA argument: None
FAILED scikitplot/seaborn/tests/test__confusion_matrix.py::TestEvalplot::test_ax_param_respected - ValueError: Invalid RGBA argument: None
FAILED scikitplot/seaborn/tests/test__confusion_matrix.py::TestEvalplot::test_kind_case_insensitive - ValueError: Invalid RGBA argument: None
FAILED scikitplot/seaborn/tests/test__confusion_matrix.py::TestEvalplot::test_kind_none_defaults_to_all - ValueError: Invalid RGBA argument: None
FAILED scikitplot/seaborn/tests/test__confusion_matrix.py::TestEvalplot::test_minimum_dataset - ValueError: Invalid RGBA argument: None
FAILED scikitplot/seaborn/tests/test__confusion_matrix.py::TestEvalplot::test_multiclass_no_crash - ValueError: Invalid RGBA argument: None
FAILED scikitplot/seaborn/tests/test__confusion_matrix.py::TestEvalplot::test_returns_axes_all - ValueError: Invalid RGBA argument: None
FAILED scikitplot/seaborn/tests/test__confusion_matrix.py::TestEvalplot::test_returns_axes_classification_report - ValueError: Invalid RGBA argument: None
FAILED scikitplot/seaborn/tests/test__confusion_matrix.py::TestEvalplot::test_returns_axes_confusion_matrix - ValueError: Invalid RGBA argument: None
FAILED scikitplot/seaborn/tests/test__decile.py::TestDecileplot::test_accepts_numpy_arrays - ValueError: Invalid RGBA argument: None
FAILED scikitplot/seaborn/tests/test__decile.py::TestDecileplot::test_ax_param_respected - ValueError: Invalid RGBA argument: None
FAILED scikitplot/seaborn/tests/test__decile.py::TestDecileplot::test_kind_case_insensitive - ValueError: Invalid RGBA argument: None
FAILED scikitplot/seaborn/tests/test__decile.py::TestDecileplot::test_kind_cumulative_gain_returns_axes - ValueError: Invalid RGBA argument: None
FAILED scikitplot/seaborn/tests/test__decile.py::TestDecileplot::test_kind_cumulative_lift_returns_axes - ValueError: Invalid RGBA argument: None
FAILED scikitplot/seaborn/tests/test__decile.py::TestDecileplot::test_kind_cumulative_response_returns_axes - ValueError: Invalid RGBA argument: None
FAILED scikitplot/seaborn/tests/test__decile.py::TestDecileplot::test_kind_decile_wise_gain_returns_axes - ValueError: Invalid RGBA argument: None
FAILED scikitplot/seaborn/tests/test__decile.py::TestDecileplot::test_kind_decile_wise_lift_returns_axes - ValueError: Invalid RGBA argument: None
FAILED scikitplot/seaborn/tests/test__decile.py::TestDecileplot::test_kind_df_has_cumulative_lift - ValueError: Invalid RGBA argument: None
FAILED scikitplot/seaborn/tests/test__decile.py::TestDecileplot::test_kind_df_has_decile_col - ValueError: Invalid RGBA argument: None
FAILED scikitplot/seaborn/tests/test__decile.py::TestDecileplot::test_kind_df_returns_dataframe - ValueError: Invalid RGBA argument: None
FAILED scikitplot/seaborn/tests/test__decile.py::TestDecileplot::test_kind_ks_statistic_returns_axes - ValueError: Invalid RGBA argument: None
FAILED scikitplot/seaborn/tests/test__decile.py::TestDecileplot::test_kind_none_defaults_to_report - ValueError: Invalid RGBA argument: None
FAILED scikitplot/seaborn/tests/test__decile.py::TestDecileplot::test_kind_report_returns_axes - ValueError: Invalid RGBA argument: None
FAILED scikitplot/seaborn/tests/test__decile.py::TestDecileplot::test_n_deciles_5 - ValueError: Invalid RGBA argument: None
FAILED scikitplot/seaborn/tests/test__model.py::TestModelplot::test_ax_param_respected - ValueError: Invalid RGBA argument: None
FAILED scikitplot/seaborn/tests/test__model.py::TestModelplot::test_kind_case_insensitive - ValueError: Invalid RGBA argument: None
FAILED scikitplot/seaborn/tests/test__model.py::TestModelplot::test_kind_none_does_not_raise - ValueError: Invalid RGBA argument: None
FAILED scikitplot/seaborn/tests/test__model.py::TestModelplot::test_minimum_dataset - ValueError: Invalid RGBA argument: None
FAILED scikitplot/seaborn/tests/test__model.py::TestModelplot::test_multiclass_no_crash - ValueError: Invalid RGBA argument: None
FAILED scikitplot/seaborn/tests/test__model.py::TestModelplot::test_returns_axes - ValueError: Invalid RGBA argument: None
48 failed, 102 passed, 1 warning in 4.54s
EXIT_CODE=1
