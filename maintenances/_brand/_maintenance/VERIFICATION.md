# Verification

Use increasing-strength evidence: static/mutation contract; focused logo tests; negative namespace/CLI probes; mocked banner subprocess tests; live figlet with canonical fonts/cache; installed-package CLI/entrypoint checks; cross-platform rendering/font evidence. Release stays blocked while an owned finding is open or live external-tool/installed-package evidence is unavailable.
