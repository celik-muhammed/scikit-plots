# Maintenance and skill revision

Scope: `maintenances/cexternals/_annoy` and `skills/cexternals/_annoy` only.
The sample's public `maintenances/annoy` files are retained unchanged.
Copy the ZIP's root directories into the full source checkout. Start at
[MAINTAINING.md](../MAINTAINING.md). No runtime files are included in this overlay.

- Repair repository discovery, header resolution and structured import checks.
- Enforce complete review lanes, safe metadata paths and independent consumers.
- Validate exact source inventory, current evidence hashes and captured log hashes.
- Add skill discovery frontmatter and canonical fresh-chat handoff.
- Preserve historical sample claims as history rather than current test results.
- Cover closed pipes, invalid profiles, stale evidence, aliases, undeclared
  consumers and header forks with 31 isolated maintenance behavior tests.
- Keep native/runtime verification outside this maintenance-only pass. Optional
  native probes distinguish compile-only, unavailable, failed and passing work.

The completed package is verified against both supplied archives. All original
runtime bytes and sibling maintenance files remain unchanged.
