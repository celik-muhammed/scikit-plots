# Corpus dependency map

No executable runtime dependency graph can be derived from this supplied snapshot because `scikitplot/corpus/` is empty. The only live information is the ownership boundary declared in `MAINTENANCE.json` / `FAMILY.md`.

Do not regenerate a graph from maintenance prose and call it source truth. Once the runtime is restored, derive imports/adapters from code and update `DEPENDENCY_GRAPH.json` with evidence.
