# `_sphinx_ai_assistant` Active Registry

| ID | Severity | Status | Finding / goal | Evidence | Exact next action | Closure gate |
|---|---|---|---|---|---|---|
| `AIA-001` | **P0** | **OPEN** | Current maintenance target claims must be reconciled with current source | Existing docs contain desired server-only secret policy while source has client token/config escape hatches. | B00/B02: annotate current vs target truth with source evidence. | no desired invariant mislabeled HOLDS |
| `AIA-002` | **P0** | **WITHDRAWN** | Canonical representation ownership must move to `_sphinx_llm` | The premise is retired: `_sphinx_llm` is frozen and the assistant's own `build-finished` generation is the intended design, not a migration debt. | None. Ownership is recorded in RULESET 16-20 and INTEGRATION_CONTRACT.md. | producer contract exit criteria |
| `AIA-003` | **P0** | **CLOSED (RUN 4)** | Prompt authority must be server-owned | Bundled HF/Cloudflare/dev proxies and the direct `_hf_spaces_model` service advertise/parse `scikitplot-chat-v1`, reject client `system`/`developer`/`messages`/tool authority, and construct system policy server-side at each bundled authority boundary. | Preserve B20 authority gates; custom endpoints remain explicitly outside the bundled trust boundary. | client cannot select authoritative system role |
| `AIA-004` | **P0** | **CLOSED (RUN 4)** | Destination-bound credential policy | `HF_TOKEN` is Path-3-only; Path 1 and Path 2 use separate `BACKEND_AUTH_TOKEN` / `HF_SPACES_AUTH_TOKEN`; credential destinations reject unsafe schemes/userinfo/query/fragment and credential-bearing redirects do not auto-follow. | Preserve B20 routing matrix and redirect gates. | malicious destination receives no credential |
| `AIA-005` | **P0** | **CLOSED (RUN 11)** | CORS parity across bundled proxy/model/worker/dev paths | B27 sets exact production defaults and rejects explicit disallowed browser Origin before expensive/write handlers; missing Origin remains server-to-server compatible and is not auth. | Preserve B05/B27 matrix; wildcard only as explicit insecure escape hatch. | `test_b05_b06_release_security.py` origin matrix |
| `AIA-006` | **P0** | **CLOSED (RUN 3)** | Share read/edit authorization separation | HF + Cloudflare now generate a distinct edit capability, store only its digest, and require `X-Share-Edit-Token` for PATCH/DELETE; public read ID alone is insufficient. | Preserve B19 server + client capability gates. | read locator cannot PATCH/DELETE |
| `AIA-007` | **P0** | **CLOSED (RUN 4 BUNDLED INFERENCE PATHS)** | Direct model/relay bypass policy | HF proxy, Cloudflare, local dev, and direct model-service paths enforce the typed contract/model policy; Path 2 preserves the structured envelope so the model service revalidates authority independently; reserved stubs are intercepted before credentials. | Preserve cross-path B20 tests; CORS/resource/logging remain separately tracked. | cross-path security matrix |
| `AIA-008` | **P0** | **CLOSED (RUN 6 CORE BOUNDARY)** | Feedback/training poisoning/provenance boundary | B22 separates opt-in minimal feedback telemetry from explicit contribution; contribution is quarantined/untrusted until independent review promotes `trainingStatus=eligible`. | Preserve B22 provenance/quarantine/training-eligibility gates; durable multi-replica review control plane is a residual, not a poisoning bypass. | Run 6 privacy + dataset fail-closed + mutation tests |
| `AIA-009` | **P1** | **CLOSED (RUN 11 APP BOUNDARY)** | Trusted forwarded identity boundary | All HF rate-limited public routes use one helper: direct peer by default; XFF only after explicit trusted-ingress declaration. Worker uses edge `CF-Connecting-IP` on direct edge traffic. | Revalidate if ingress topology changes; do not treat IP as authenticated human identity. | B27 cross-route spoof/identity gate |
| `AIA-010` | **P1** | **CLOSED (RUN 11 APP BOUNDARY)** | Pre-buffer resource limits | HF proxy/model and Worker enforce declared + incremental byte ceilings before full application buffering; loopback dev proxy checks declared length before read. | Keep hard application ceilings even when platform ingress allows larger requests. | B27 declared/chunked oversize tests |
| `AIA-011` | **P1** | **OPEN** | Settings/config schema consolidation | 90 Sphinx config calls plus browser registries create drift/sensitivity risk. | B10 evaluate/promote schema-driven registry after security/representation migration. | single ownership record; secret class nonserializable |
| `AIA-012` | **P1** | **OPEN** | Known monolith decomposition | 22k JS / 14k CSS / 5.5k Python contain multiple responsibilities. | B11 extract only after tests pin behavior. | no new responsibility mixing + green gates |
| `AIA-013` | **P1** | **WITHDRAWN** | Static Markdown consumer migration | No external producer to migrate to; the assistant is the producer. | None. B08 re-scoped to *own* the representation. | producer contract exit criteria |
| `AIA-014` | **P2** | **OPEN** | Corpus/MCP boundary integration | Future retrieval/MCP features should consume existing contracts, not duplicate them. | B12 after core security/representation stabilization. | boundary/import tests |
| `AIA-015` | **P1** | **CLOSED** | Streaming intent was treated as guaranteed SSE, allowing JSON-only upstream completions to become empty browser answers | Proxy v6.4 opens upstream before committing success, preserves JSON as JSON, preserves SSE as SSE, and emits structured terminal errors. `tests/test_proxy_streaming_state.py`. | Keep `APP_STREAMING_RUNBOOK.md` and the streaming regression suite synchronized. | JSON-under-stream, true-SSE, pre-header error, mid-stream error, empty-body gates |
| `AIA-016` | **P1** | **CLOSED** | Reserved stub models could escape into real routing when the diagnostic stub was disabled | v6.4 reserves `stub/*` before Path 1/2/3; disabled stub requests return local 503 and perform no upstream call. | Preserve fail-closed namespace and blank-Authorization regression tests. | disabled-stub isolation + no blank Bearer header |
| `AIA-017` | **P2** | **CLOSED** | Maintenance plans/backups/task notes were colocated with the runtime extension | Maintenance control plane moved to `maintenances/_externals/_sphinx_ext/_sphinx_ai_assistant/`; runtime-local `tasks/` removed and drift checker enforces separation. | Keep repository-level mirrored maintenance path; do not reintroduce runtime maintenance imports. | maintenance checker GREEN in repository layout |
| `AIA-018` | **P1** | **CLOSED** | Share conversation duplicated format-specific sheet/state routing and synchronized repeated controls through singleton DOM authority | B16 replaces three JSON/HTML/TXT sheets with one registry-driven Share shell, lazy immutable panels, observer-based export-mode controls, explicit conversation-state identity, and a canonical sheet registry. | Preserve the B16 source + execution + mutation gates; new formats extend registry capabilities rather than hard-coded sheet dispatch. | `test_share_conversation.mjs` 97/97 + `test_share_conversation_dom.mjs` 35/35 + mutation gate |
| `AIA-019` | **P2** | **PARTIAL (RUN 8 PAYLOAD PREFLIGHT LANDED)** | Share payload transport limits and real-browser responsive/focus behavior require acceptance gates | Run 8 adds UTF-8 size summary, 48 KiB warning / 256 KiB block budgets, responsive Share CSS, keyboard/source/fake-DOM gates, and safe fallbacks; representative browser focus/layout/clipboard acceptance is still not proved. | Add maintained real-browser Share workflow coverage across representative viewport/theme/focus/clipboard states before closing. | threshold/mutation tests + browser E2E + documented fallback behavior |

| `AIA-020` | **P0** | **CLOSED (RUN 3)** | Export/Share active-content isolation spans browser serializers/self-contained transport and the Global Share server representation boundary | Run 2 closed browser raw-text/c1 execution; Run 3 makes HF + Cloudflare accept structured snapshots only and render allowlisted JSON/HTML/Text/YAML/TOML server-side with sandbox/no-store policies. | Preserve hostile-content, server-authority, and mutation gates; extend the same contract when YAML/TOML land. | hostile fixture inert in browser paths AND direct API cannot host caller-selected active MIME/content |
| `AIA-021` | **P0/P1** | **CLOSED (RUN 1)** | Endpoint Share/Feedback bearer values could persist in localStorage or be baked into static Sphinx config | Endpoint storage schema v3 rewrites legacy blobs without token fields; Python strips explicit/legacy build-time token values. | Preserve session-only runtime compatibility only; production auth remains server-side. | `test_endpoint_secret_lifecycle.mjs` + `test_client_secret_boundary.py` + mutation positives |
| `AIA-022` | **P0/P1** | **PARTIAL (RUN 14 LEGACY DRAIN BOUNDED)** | Capability/private identifiers and unsanitized exception text can reach operational or infrastructure logs | Runs 3/5 remove capabilities/content from app events; Run 13 makes current URLs/operations capability-safe; Run 14 stamps generation 2, blocks those objects on legacy paths, retires legacy PATCH, and makes fixed update a one-way migration. Historical pre-generation URLs can still expose their locator until expiry/revoke/migration. | Remove legacy route code after the pre-generation population drains; disable/redact any full-body/WAF/packet telemetry that can capture locator bodies. | B21/B29/B30 logging + fixed-transport + generation-drain gates |
| `AIA-023` | **P1** | **PARTIAL (RUN 16 SHARED AUTHORITY LANDED)** | Personal/sensitive conversation data can be over-collected across inference/Share/feedback/contribution paths | Runs 6-7 minimize telemetry/egress; Run 12 adds lifecycle/withdrawal/current-view cleanup; Run 16 adds optional shared atomic Redis receipt authority, fail-closed shared-required mode, and ambiguity-safe promotion recovery. External Redis durability and provider-history/global erasure remain separate deployment claims. | Preserve B23/B28/B32 gates; require shared Redis authority before horizontal collection, verify its persistence separately, and never promise global erasure without provider-complete evidence. | B22/B23/B28/B32 tests + deployment durability/deletion evidence |
| `AIA-024` | **P1** | **CLOSED (RUN 7 CLIENT BOUNDARY)** | Sensitive user/page/share/contribution content could leave the browser without a local warning/review boundary | B23 adds one advisory preflight over the actual outbound inference envelope, canonical Share snapshot, and explicit contribution payload; findings contain categories/counts/codepoints only and async decisions are conversation-bound. | Preserve fail-closed/no-match-retention/race mutation gates. Never treat client detection as server authorization or complete PII classification. | `test_privacy_preflight*.mjs` + Share DOM race + mutation gate |
| `AIA-025` | **P1** | **CLOSED (RUN 8)** | Created Share links/artifacts need truthful removal/deletion capability semantics | B24 gives Local preview real Blob revocation, Global links server DELETE via private edit capability, legacy local-store deletion, explicit local-only semantics for self-contained links, and one page-memory lifecycle registry covering both Share-sheet and direct toolbar Downloads. | Preserve artifact lifecycle DOM + mutation gates; never relabel local forget as remote revoke. | `test_share_conversation*.mjs` + `test_global_share_capability.mjs` + Run 8 lifecycle mutants |

| `AIA-026` | **P1** | **CLOSED (RUN 9)** | Global links handed to the user could become invisible to artifact management after new-chat/reload because only one current public Share state was recoverable | B25 adds a 25-entry session-scoped public Global ledger, read-only restore, explicit HEAD status checks, revoke tombstones, and truthful Forget semantics without persisting edit capability. | Preserve Run 9 source/DOM/server/mutation gates; do not trade secret persistence for cross-reload Revoke. | multiple provided links restore read-only; edit token absent; revoke/expire/forget lifecycle transitions proved |
| `AIA-027` | **P1** | **CLOSED (RUN 10)** | Legacy/tampered Global recovery could rehydrate forbidden fields; reason-unknown 404 could be treated destructively while stale current PATCH state survived status failure | B26 fail-closed allowlisted recovery scrub, recheckable unavailable state, current-state detach, 410→fresh-POST fallback, and explicit HF/Worker expiry parity. | Preserve Run 10 storage/DOM/server/mutation gates; never convert 404 into proof of revocation. | reload cannot restore mutation authority; 404 remains recheckable; expired routes return 410 consistently |
| `AIA-028` | **P2** | **PARTIAL (RUN 15 AUTHORITY IMPLEMENTED; HF ACTIVATION DEPLOYMENT-CONDITIONAL)** | Distributed request quotas need one shared atomic decision plane across claimed replicas/PoPs | B31 adds sharded Worker Durable Object authority and optional atomic Redis authority for HF; local/KV paths remain explicitly soft compatibility gates. Bundled Worker requires DO authority, but HF stays local by default for single-instance compatibility. | Horizontal HF deployments: provision one intended Redis consistency domain + dedicated HMAC key and set `RATE_LIMIT_BACKEND=redis` + `RATE_LIMIT_REQUIRE_SHARED=true`; verify health says shared/authoritative/ready. | B31 runtime tests + deployment configuration/health evidence |

| `AIA-029` | **P1** | **PARTIAL (RUN 16 SHARED COORDINATION IMPLEMENTED; DEPLOYMENT DURABILITY CONDITIONAL)** | Horizontal contribution replicas need one atomic receipt/review/delete authority without unsafe exactly-once assumptions about external provider writes | B32 adds a shared Redis receipt ledger, HMAC receipt keys, shared-required fail-closed behavior, claim digests/leases, non-promotable `promotion_uncertain`, and monotonic withdrawal. Redis cannot fence an already-issued Git/provider mutation and repository code cannot prove Redis persistence policy. | Horizontal deployments: configure one intended Redis domain + dedicated contribution HMAC key + `CONTRIBUTION_REQUIRE_SHARED=true`; verify Redis TLS/ACL/persistence/recovery separately and reconcile uncertainty operationally or toward withdrawal. | B32 cross-replica + ambiguity + fail-closed tests plus deployment Redis evidence |

| `AIA-030` | **P1** | **CLOSED (RUN 16.2)** | Self-contained and Global Share creation could succeed without a reliably portable/copyable user-facing link | B33 makes current self-contained output a reviewed inert bounded base64 data URL and requires successful Global creation to resolve/display/register a validated public read URL, including UUID-only compatible responses. | Preserve B33 active-content/DOM/source/global transport gates; never describe base64 as encryption or copied data URLs as revocable. | data URL inert + UUID-only Global response yields copyable fixed-viewer link |

| `AIA-031` | **P1** | **CLOSED (RUN 16.2.6 CODE BOUNDARY; LOCAL-FILE ACTIVATION OPTIONAL)** | HF internal HTTP metadata could prevent public HTTPS Share-link creation, while standalone `file://` pages were indistinguishable from other opaque `Origin:null` documents | B35 uses validated HF `SPACE_HOST` as deployment-owned public-origin evidence and adds an explicit Share-only opaque-origin opt-in. Arbitrary remote HTTP bases and non-Share opaque-origin requests remain denied. | Deploy proxy 6.5.2+. For local-file Global Share only, set `SHARE_ALLOW_OPAQUE_ORIGIN=true` with awareness that opaque origin is not authentication. | internal-HTTP HF request yields external HTTPS link; null-origin Share succeeds only under opt-in; health/non-Share remains denied |

| `AIA-032` | **P1** | **CLOSED (RUN 17 B36)** | Dataset contribution was hidden under Share → More actions while feedback used ambiguous “Save to dataset” wording, conflating telemetry, human sharing and content donation | Dedicated Contribute sheet; separate telemetry/Q&A shortcut; schema-v4 conversation record; exact inspect/privacy/consent flow; Share owns no contribution controller | Preserve B36 source/DOM/schema/mutation gates and purpose-specific UI copy | telemetry remains content-free; Share section has no contribution authority; all contribution scopes enter one quarantine lifecycle |

| `AIA-033` | **P1** | **CLOSED (RUN 17 B36)** | Feedback telemetry was client-default-off but server acceptance lacked explicit consent evidence, and the public feedback DOM event exposed the content-rich local tuple to page listeners | Versioned structured browser permission, self-gated rating/retraction helpers, HF/Worker schema-4 consent gate, content-free public feedback event | Preserve AIA-C32 execution/server/mutation gates; operator persistence must never manufacture reader consent | legacy/malformed/stale consent stays Off; no network after opt-out; public event contains rating mechanics only |

## Priority order

Security authority and producer/consumer boundaries precede monolith refactoring. Do not restructure large files first and hope the security/representation architecture becomes clearer afterward.
| `AIA-024` | **P1 / release** | **PARTIAL (RUN 20 BINDING CLOSED; EXTERNAL FACTS OPEN)** | Production scanner/SBOM/provenance/Redis/logging evidence could be stale or attached to the wrong source/artifact | B39 adds a <=72h content-addressed evidence envelope bound to exact runtime source, lock, base manifest and final image; infrastructure telemetry/Redis operational guardrails are fail-closed. | Produce trusted fresh external evidence in release CI/operations; keep `SEC-P1-38` open until then. | `verify_release_gate.py` GREEN against concrete production evidence + external trust-root checks |

## Run 24 / B43

- checkpoint: `checkpoints/B43_BOUNDED_REMOTE_RESPONSE_CONTEXT_INGESTION.md`
- fresh-chat handoff: `FRESH_CHAT_BOUNDED_REMOTE_RESPONSE_CONTEXT_INGESTION_HANDOFF.md`
- primary executable gates: `tests/test_run24_bounded_remote_response.py`, `tests/test_run24_bounded_remote_response.mjs`
- scope: pre-buffer remote-response limits across browser chat/control/canonical reads, isolation reads, Global Share viewer, HF/dev chat relay and Worker chat relay


## Run 25 / B44

- `AIA-034` / `SEC-P1-46`: **CLOSED at repository-controlled provider-control boundary** — storage mutations no longer buffer unused provider bodies; metadata JSON is bounded before parse; scoped HF SDK transport is bounded; GitLab custom API authority is validated and unsupported provider `api_base` is rejected.
- `AIA-035` / `SEC-P1-47`: **CLOSED at deterministic rendered-context boundary** — same-origin and isolated ingestion use live rendered DOM style/geometry before clone serialization.
- `SEC-P2-48`: **OPEN residual** — overlay/z-index occlusion, animation/timing tricks, and proof of reader attention are not claimed.
- `SEC-P2-49`: **OPEN scoped bulk-transfer residual** — intentional large model/dataset/provider downloads are not governed by the small control-response ceiling.

## Run 35 / B54

| ID | Severity | Status | Finding / goal | Evidence | Exact next action | Closure gate |
|---|---|---|---|---|---|---|
| `AIA-036` | **P1** | **CLOSED FOR SUPPORTED PROVIDERS WHEN `provider-pr` ENABLED** | Process-local quarantine had no practical maintainer review UI and promotion required an opaque receipt ID. | B54 maps Primary HF/GitHub/GitLab/Bitbucket storage to native PR/MR review; canonical merge is training eligibility; user deletion and manual provider merge are reconciled. | Deploy `CONTRIBUTION_REVIEW_MODE=provider-pr` with least-privilege provider token; keep dataset repo private when submissions are non-public. | B54 provider adapter/lifecycle tests + provider deployment smoke test |
| `AIA-037` | **P2** | **OPEN DEPLOYMENT/REPLICATION RESIDUAL** | Native review makes the Primary provider durable/reviewable but does not prove local ledger durability or synchronous mirror convergence after a human merge. | Primary provider is explicitly the sole review authority; B32 durability policy still governs receipt/delete-capability coordination and mirrors remain replicas. | Use Redis/SQLite durable ledger as appropriate; add explicit post-merge mirror reconciliation only if mirrors are required as immediate eligible replicas. | deployment durability evidence + mirror convergence test if enabled |
