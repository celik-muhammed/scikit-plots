# R172 — local debugging handoff and maintenance/skill normalization

Status: **IN PROGRESS** until the relocated-path focused tests pass.

## Objective

Make a fresh chat capable of continuing from Run 172 without conversation
history, while separating runtime, maintenance, and skill planes.

## Changes

- created a real top-level mirrored `maintenances/.../_sphinx_ai_assistant` plane;
- reduced the skill plane to `SKILL.md` plus tiny entry documentation;
- moved old fresh-chat handoffs to maintenance history;
- moved maintenance-only checker/dev proxy/example Python out of runtime/skill clutter;
- moved feedback/dataset operator guides beside `_hf_spaces_proxy`;
- reduced runtime-root Markdown to README, isolation, and activity/file preview;
- removed generated bytecode/cache/backup debris;
- replaced stale current `STATE.json` and `TRACKER.json` with Run 172-aware state;
- documented the next mode as local test-first debugging, not new feature work.

## Acceptance

- maintenance checker GREEN;
- focused dev-proxy privacy/limit/stub tests GREEN;
- feedback/dataset documentation tests GREEN;
- no runtime import/reference to maintenance as a Python dependency;
- no cache/bytecode/backup artifacts;
- runtime root has exactly three Markdown documents;
- a fresh chat can follow `SKILL.md` + `FRESH_CHAT_HANDOFF.md` without prior history.
