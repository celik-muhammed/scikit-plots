# Archived test-only ExtSettings prototype

These files were removed from canonical tests during the mirrored-test migration because `_ext_settings.js` is not a delivered runtime module and `test_ext_settings.js` was not collected by the current pytest/Node release gate. They are retained only as historical design evidence.
