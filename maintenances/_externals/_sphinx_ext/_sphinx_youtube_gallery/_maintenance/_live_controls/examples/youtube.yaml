# Catalog for learn/resources/youtube/.
#
# Generated by, and refreshable with:
#
#   python -m scikitplot._externals._sphinx_ext._sphinx_youtube_gallery.sync \
#       --source <any YouTube URL> --output <this file>
#
# Hand edits under handle/tags/fields are preserved by video id. Provider
# metadata refreshes from YouTube, and unchanged normalized output is not rewritten.
# Without YOUTUBE_API_KEY the RSS refresh is partial/recent-only, so unseen
# older records are retained rather than destructively pruned.
#
# Only `id` is required. Everything else improves what you can do with
# :sort:, :group-by:, :since: and :until:.

videos:
  # Migrated from the previous hand-written "Videos" bullet list.
  - id: JXtISpdDPNY
    title: >-
      Principal Component Analysis in Python | How to Apply PCA |
      Scree Plot, Biplot, Elbow & Kaisers Rule
    channel: Statistics Globe
    handle: StatisticsGlobe
    tags: [pca, dimensionality-reduction]
