# `scikitplot.memmap` maintenance control plane

Start at `../MAINTAINING.md` or `FRESH_CHAT_HANDOFF.md`. Deterministic checks live in `tools/`; regression tests live in `tests/`; executed evidence logs live in `evidence/`. `history/` is non-authoritative provenance.
