# Physical tracker

Machine-derived runtime inventory and fingerprint live in `TRACKER.json`.
Refresh is intentionally refused while structural runtime checks fail, so the
current missing-generator defect cannot be normalized away with `--update`.
