# Fresh-chat handoff

Start with `STATE.json`, then run `check_trackers.py --json`, `review_subsystem.py --json`, and the maintenance tests. Current runtime is intentionally red. Preserve the distinction between a Keras-only isolation harness using fake aggdraw and real package/rendering integration. The highest-priority runtime repair is `VKR-GRAPH-001`; add a single-output regression before changing shape normalization.

Do not edit TensorFlow/Keras internals or root PIL utilities from this domain. Any change to framework traversal must be tested with standalone Keras and TensorFlow separately where available.
